[project]
name = "game-two-assets-tools"
version = "0.1.0"
requires-python = ">=3.12"
description = "Deterministic validation tools for game-two visual assets"

[tool.ruff]
line-length = 100
target-version = "py312"
