# Selection register — mechanical record of owner-ratified asset selections

Dated records of owner decisions that fix a selected candidate as the
of-record choice for this repo's FUTURE compositions. This register is
mechanical (the docs/owner-redirects.md pattern): each entry names its
date, the owner wording verbatim, the carrier (where the adjudicated
evidence lives), the exact consequence, and the reversal cost — no lore,
no narrative, no fiction names (repo invariant 5). An entry here moves
zero pixels: banked artifacts keep regenerating from their pinned
history, releases stay byte-frozen, and integration remains a separate,
owner-sequenced decision at the hub.

- **2026-08-21 — k0 strike key: the remedy-v15 K-S pair is the selected
  k0-of-record for future compositions in this repo.** Owner ruling,
  verbatim, at v15 close: **"Approved, proceed"** — ratifying the v15
  receipt (the K-S selection verdict) and authorizing the recording
  sprint. Selected assets: `player_1_lane_b_attack_down_k0_ks` +
  `player_1_lane_b_attack_right_k0_ks` (release `remedy-v15`,
  `exports/remedy-v15/release.json`, SHA-256-pinned). Carrier:
  `reviews/remedy-v15/verdict.md` (the selection adjudication under the
  pre-registered decision rule) + the v16 sprint brief (the approval
  wording) + `reviews/adoption-v16/` (the sighting-context demo proof).
  Consequence, scoped: review compositions authored in this repo AFTER
  this entry draw the K-S bytes for the k0 slot unless the composition's
  own pre-registration declares otherwise; `exports/calibration-v2` k0
  remains the pinned regeneration history every banked `--check` and
  banked artifact rebuilds from — banked history is untouched; no
  game-two integration is implied, scheduled, or requested. Reversal:
  delete this entry (one line of history in this file; zero pixels move;
  the incumbent was never destroyed). If reversed, DEF-2 forwards to the
  temporal register exactly as v14 proposed (P1-bundle falsifiable) —
  the v15 contingency note, retained verbatim.

Dispositions recorded by later verdicts (e.g. the DEF-2 contingency
resolution) are appended here by the sprint that adjudicates or records
them, never pre-written.
