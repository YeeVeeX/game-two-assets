# Calibration v8 — walk→attack onset-seam verdict (mid-walk onset classes)

Reviewed artifacts:

- `reviews/calibration-v8/seam-sheet.png` — SHA-256
  `a426b602bf03653e41e5f07baab496ea6a53efe50a2db0618218951940cd3245`,
  regenerated byte-identically (in-process double-build in
  `seam_metrics --check` plus a full CLI re-run compared with `cmp`).
- `reviews/calibration-v8/seam-metrics.json` — SHA-256
  `fc4bd4bae36912b7f3f223408a4d7aa3d1a62640853bdd26a094df499e49cc99`,
  regenerated byte-identically under identical flags (verified again
  after an owner-side power interruption mid-session — the committed
  bytes equal a post-restart fresh build).
- Viewing aids (non-blocking): `seam-lanes-down.apng` (`059be50e…`) and
  `seam-lanes-right.apng` (`0688c112…`) — the four onset lanes side by
  side, 34 frames, exact 1/60 s per-frame delay via the banked APNG
  encoder, 0.5 s final hold, infinite loop, 4x nearest-neighbor;
  byte-identical on regeneration and equal to committed bytes
  (machine-checked).
- **No release, no new pixels:** this sprint composed banked bytes only.
  All 26 banked export pins across the seven releases (v0–v3, v5–v7)
  byte-verified; `exports/` contains exactly the pinned files and no
  `calibration-v8` directory; the `make_release.py` registry and
  `tools/export_assets.py` are untouched (machine-enforced, the
  zero-new-exports bar).
- Baseline: two mechanical identity re-pins this sprint (`766cfa2e…` at
  step 0; `5850df13…` at the banking checkpoint — v18 soak/session-9
  docs activity; all five pinned hashes LF-identical at each,
  `attack_timing` re-verified 5/4/8/13 at each, sprint-8 unpinned deps
  — `controllers.rb`, `grid_walker.rb`, `window.rb` — zero-diff across
  both hops; `start_attack` still carries no `moving?` guard, so the
  seam class under test is reachable at the pin). Both committed alone.
  Live gate exit 0 at step 0 and immediately before banking.
- Vision-critical judgment: session model `us.anthropic.claude-fable-5`
  (verified from `PI_MODEL`, not self-report), on the committed sheet
  plus deterministic band crops of the same bytes (ephemeral
  diagnostics; native 1x decides). Council seat cross-vendor (Kimi
  K2.5), one consolidated adversarial verdict delivered across a
  truncation continuation (4,494 tokens total of the 8k cap: 1,698/1,900
  + 425/471), responses redirected to files and read as UTF-8.

Question under test (the banked v7 alternative next-hypothesis): when an
attack begins MID-WALK — legal in the engine, verified at the pin — do
the onset seams read at native 1x, per tick, without weakening any
banked read, and does the strike keep its salience when it fires on a
moving base? Zero new frames; per-onset-class sub-verdicts; FAIL routes
to an ENGINE-TERRITORY finding (input-buffering/gating recommendation),
never a rescue frame.

## Accuracy — all-must-pass

| Item | Verdict | Evidence |
|---|---|---|
| Zero new exports; 26/26 banked pins byte-verified; no v8 exports dir; exporter/registry untouched | PASS | `seam_metrics --check` zero-new-exports bar (the rationale corrected the brief's "24" against release.json ground truth: v0 banks 6 idles across lanes a/b/c) |
| Seam jump tables complete: onset−2..onset+10 (EARLY +12), pose Δ% + position Δpx per consecutive pair, tween+offset positions | PASS | committed metrics, 4 lanes × 2 facings (14 rows EARLY, 12 others) |
| **CONTROL regression (hard):** onset row = banked 16.48/19.69 at −3px; shared rows equal committed v7 timeline-B values exactly | PASS | machine-compared against `reviews/calibration-v7/timeline-metrics.json` rows (14,15), (15,16), (19,20), (23,24), (24,25) — pose AND position |
| **Release-salience bar:** a0→k0 strictly the largest pose delta AND strictly the largest \|position\| delta in every lane, both facings | PASS | release 27.18/31.97 at +13/+12/+9/+9 vs in-lane maxima ≤ 23.49 down / ≤ 28.27 right (pose) and ≤ 6px (position); dominance on both axes separately, no combined metric |
| Tick math exact: windup 5 (w0×1 + a0×4) at −3, active 4 (k0) at +6, recovery 8 (s0×1 + r0×6 + x0×1) at 0, banked walk-frame mapping, tween deltas 1,1,2,3,4,3,4,3,4,3,2,1,1 (peak 4px) | PASS | `seam_metrics --check`, 0 failures; the r0-hold floor (6 held ticks) untouched by construction in every lane |
| Overlap arithmetic: REM 11/8/4/0; arrival (t14) lands rec 3 / act 4 / wind 5 / pre-onset; moving-base tick counts 5+4+2 / 5+3 / 4 / 0 | PASS | machine-recomputed per lane; matches the pre-registered class table |
| Compositor byte-determinism (sheet, metrics, both APNGs) | PASS | double-build SHAs above; CLI re-run `cmp` clean; committed bytes equal fresh builds |
| Composition purity: every creature cell = banked export bytes at computed integer offsets | PASS | 340 cells, dual verification (region reconstruction + direct export-byte equality), 0 failures |
| Banked sheets v0–v7 regenerate byte-identical under the extended toolchain; coverage ≥ 80 | PASS | full gate 290 tests green; coverage 94% total (new tools 99% / 85%) |
| Pass bars pre-registered before artifacts existed | PASS | rationale + toolchain committed (`9c0d4c2`, change set 1) before the sheet was generated (change set 2) |
| No game-two changes | PASS | read-only `git show` against `../game-two` only |

**Accuracy: 11/11.**

## Presentation — onset-seam rubric at native 1x

Measured evidence (`tools/seam_metrics.py`; pose delta =
100·XOR/union on export bytes; positions = independently recomputed
smoothstep + pinned state offset; both facings share the position
columns):

| Onset-seam rows (pose Δ% down/right / Δpx) | EARLY (t03) | MID (t06) | LATE (t10) | CONTROL (t15) |
|---|---|---|---|---|
| pre-onset context | idle→f0 15.87/10.29 /+1 | f0→f0 0 /+3 | **f1→f2 19.00/22.09 /+3** | f3→f3 0 /+1 |
| **fN→w0 onset cut** | f0→w0 **22.91/28.27** /−2 | f0→w0 **22.91/28.27** /+1 | f2→w0 **22.91/28.27** /+1 | f3→w0 **16.48/19.69** /−3 |
| w0→a0 | 17.25/20.83 /+2 | /+3 | /+3 | /0 |
| a0 hold drift (per tick) | +3,+4,+3 | +4,+3,+4 | +2,+1,+1 | 0 |
| **a0→k0 release** | 27.18/31.97 /**+13** | /**+12** | /**+9** | /**+9** |
| k0 hold drift | +3,+4,+3 | +2,+1,+1 | 0 | 0 |
| k0→s0 | 15.38/21.31 /−4 | /−6 | /−6 | /−6 |
| s0→r0 | 16.18/14.98 /+1 | /0 | /0 | /0 |
| arrival lands | recovery tick 3 | active tick 4 | windup tick 5 | pre-onset |

Strike-anchoring geometry (derived from the committed plan positions +
banked k0 bboxes — down [8,6,23,27], right [7,7,28,27]; window
coordinates put the arc-tile grid line at 64; pixels-inside = leading
edge − 63; the CONTROL values reproduce the banked v4 anchoring — 2px
down / 3px right inside the target tile — an independent cross-check of
the derivation):

| Active ticks crossing the arc grid line | EARLY | MID | LATE | CONTROL |
|---|---|---|---|---|
| down | **0/4** (edge −13,−10,−6,−3 short) | 2/4 (last two: 1–2px in) | 4/4 (2px in) | 4/4 (2px in) |
| right | **0/4** (−12,−9,−5,−2 short) | 3/4 (0–3px in) | 4/4 (3px in) | 4/4 (3px in) |

Rubric line by line (pre-registered; judged on the committed sheet at
native 1x, both zones, both facings, with the 2X onset/release strips
and band crops for diagnosis; every claim is sheet-scope — per-tick
legibility and single-tick recognizability under identical pinned
timing, never temporal fusion at 60 tps):

1. **The fN→w0 cut reads as attack onset, not error/flicker/new
   state.** PASS at sheet scope, all four classes, both facings, both
   zones. The mid-walk cuts (22.91/28.27) land ON the banked bridge
   pose: the head-drop-plus-haunch (down) and neck-compression (right)
   change the silhouette CLASS from stride to gather, and in LATE the
   adjacent real walk cut (f1→f2, 19.00/22.09) sits one column earlier
   as a built-in noise comparison — the onset cut visibly changes shape
   family where the walk cut only re-arranges legs. Two honest
   magnitude disclosures, held to at 1x: (a) the right-facing onset
   cuts (28.27) sit inside the magnitude band v6 judged bridge-worthy
   on other boundaries (27.12–36.57) — they pass here because w0 IS the
   bridge (the banked doctrine decoupled magnitude from bridging via
   the deliberately-sharp 27.18/31.97 release, and the seam under test
   cuts INTO the smoothing pose, not past it); whether a 28% one-tick
   cut strobes at 16.7 ms is temporal and joins the runtime item.
   (b) The retreat cue inverts on MID/LATE: CONTROL's onset moves
   −3px (the banked pullback); MID/LATE drift +1px FORWARD (peak tween
   +4 absorbing the −3 offset). The v2-banked subthreshold finding
   (±3px offsets alone are invisible at 1x) and the v4-banked "the pose
   itself is the signal" carry the read — the pose change is the onset
   signal and it is unmistakable — but the directional cue CONTROL
   enjoys is absent mid-walk; recorded as a finding, temporal
   consequence to the runtime item.
2. **The windup/active/settle poses riding the moving base keep their
   banked reads — EXCEPT the strike's anchoring in EARLY.** PASS for
   MID, LATE, CONTROL; **FAIL for EARLY**, both facings. What holds
   everywhere: the held a0 repeated identically while gliding 1–4px per
   tick stays the coil (no leg alternation, nothing walk-like — the
   frozen-pose glide reads as a coiled body creeping); w0's sink, s0's
   collapse, r0's slump all keep their banked identities on the moving
   base (the recovery-overlap ticks judged under the DECLARED drawing
   model — the same review convention every banked timeline uses; the
   engine draws no poses, and the recovery-walk priority question stays
   a carried integration finding, adopted council wording). What breaks
   in EARLY: the banked k0 read is not just gape-and-brace, it is the
   v4-banked anchoring — "the displacement has a destination; the
   leading edge crosses into the target tile." In EARLY the strike
   fires with REM 11 and its leading edge stays 3–13px (down) / 2–12px
   (right) SHORT of the arc-tile grid line on every active tick — the
   gaped pose lunges and lands nowhere the eye can bind it: the tile
   the hit actually strikes (one ahead of the committed tile) is never
   visually reached. At 1x the EARLY active row reads as a strike
   whose destination is missing — the committed-tile lag made visible.
   MID recovers the crossing late and shallow (its last one/two active
   ticks, 0–3px inside, coinciding with arrival); disclosed as a
   watch-item, judged still-anchored at sheet scope because the
   crossing exists and lands exactly when the pose peaks.
3. **The strike keeps salience.** PASS at sheet scope, all lanes. The
   machine bar (strict dominance on BOTH axes separately) passes
   everywhere, and the native read agrees: in every lane band the
   release column is the sheet's most attention-grabbing single-tick
   event — the gape plus the largest jump (EARLY's +13px, 3.25× max
   walk speed, is the most violent event on the sheet). Scope split per
   the council (adopted): what the sheet proves is the number, the
   geometry, and the per-tick dominance; whether +13px in 16.7 ms reads
   as attack rather than teleport under motion is temporal — routed to
   the runtime item with the sample-hold-display smear check.
4. **The CONTROL lane reads exactly as banked.** PASS. Machine-equal to
   the committed v7 timeline-B rows (pose and position), and the native
   read of the CONTROL band is the banked t13–t28 segment: the −3
   pullback onset, the held coil, the +9 boundary-crossing release
   (2px/3px inside the arc tile — reproducing the banked v4 anchoring
   values from an independent derivation), the −6 recoil settle.
5. **Identity holds throughout.** PASS. Byte-frozen frames at computed
   integer positions (purity dual-verified, 340 cells), frozen ramp,
   feet/anchor law inside the frames, nearest-neighbor everywhere; the
   EARLY negative excursion (w0 at −1) keeps every opaque pixel
   in-window by the contract's own bounds margin.

**Presentation scores (accuracy scored separately above):**

- **Down lanes: 8.5/10.** Four stacked bands make onset-phase
  comparison one vertical eye movement; the EARLY anchoring failure is
  visible against the drawn grid lines — the sheet's design caught it.
- **Right lanes: 8/10.** The wide cells carry the bigger onset cuts
  (28.27) legibly; zone_2's busier floor costs a little contrast at the
  onset columns, never the read.
- **Sheet as artifact: 8.5/10.** The 2X onset strips put all four
  fN→w0 seams side by side (the noise-vs-signal comparison LATE
  provides is the strongest single diagnostic); the release strips make
  the +13/+12/+9/+9 progression literal.

## Structured critique and cross-vendor review (Kimi K2.5, adversarial)

One consolidated verdict across a truncation continuation; every
numeric claim recomputed against pixels/numbers before adoption
(models see text, not pixels). Council posture was REJECT-leaning;
reconciled:

1. **Q1 position arithmetic — REFUTED (premise error), presentation
   duty ADOPTED.** The council could not reproduce the position columns
   and flagged w0→a0 deltas as "should always be −3." Its premise is
   wrong at the pinned convention: w0 IS windup tick 1 and draws at the
   windup offset (banked v6/v7, machine-checked in both banked sheets),
   so w0→a0 position deltas are pure tween motion (0 at rest, +2/+3
   mid-walk) — and the release +13 = peak tween +4 riding the +9 offset
   jump, exactly as pre-registered. Every spot-check it attempted
   resolves under that convention; the tick-math bar recomputes all of
   it from pinned constants. ADOPTED: the evidence tables above state
   the convention explicitly — a text-only reviewer's natural misread
   is the signal that the presentation needed it.
2. **Q2 magnitude-consistency attack — PARTIALLY ADOPTED.** The council
   called accepting 28.27 unbridged "inconsistent" with v6 bridging
   27.12–29.90. REFUTED as a rule: the banked doctrine never had a
   bridge-above-N% rule — v6 banked the 27.18/31.97 release as
   deliberately sharp the same day it bridged smaller boundaries; the
   semantic role decides, and the mid-walk onset cut lands ON the
   bridge pose (w0), so the smoothing the grammar owns is already in
   the seam. ADOPTED: the magnitude-band disclosure now sits in rubric
   line 1, and the 16.7 ms strobe question for 28% cuts joins the
   runtime item.
3. **Q3 retreat-cue inversion should fail L1 — REFUTED with banked
   evidence, kernel ADOPTED.** The council read the +1px forward drift
   under a gather-back pose as a pose-motion conflict a sheet cannot
   wave through. The banked record answers the magnitude question: v2
   banked ±3px offsets ALONE as subthreshold at 1x, and v4 banked the
   pose as the signal carrier — a 1px drift under a 22.91/28.27 pose
   change is far below the banked noise floor, and the native read
   confirms the pose carries. ADOPTED: the cue inversion is a recorded
   finding (integration list) with its temporal consequence routed to
   the runtime item, and L1's PASS wording now names it rather than
   waving it.
4. **Q4 declared-model overreach — CONFIRMED as wording duty.** The
   drawing model is a review convention the engine does not implement —
   true, and true of every banked timeline since v4 (the engine draws
   primitive bodies; designing the pose system is this repo's purpose).
   ADOPTED: rubric line 2 and the findings tag every recovery-overlap
   claim as conditional on the declared model; nothing banked here
   asserts engine behavior.
5. **Q5 static sheet cannot adjudicate teleport-vs-attack — ADOPTED,
   and the pressure produced this verdict's sharpest finding.** The
   council's split (sheet proves numbers + geometry; motion reads are
   runtime) is exactly the banked v4 correction class, adopted in line
   3. Recomputing its geometry estimate ("5–6px past the line" —
   fabricated-precision class) against banked bboxes produced the
   anchoring table above: EARLY never crosses at all — which flipped
   EARLY's sub-verdict to FAIL on evidence the pre-registered rubric
   (line 2, "k0 keeps its banked read") already covered. The council
   did not name the finding; its question forced the measurement that
   did.
6. **Q6 unthought risk — ADOPTED.** Sample-hold display smear: a +13px
   single-tick displacement on a hold-type panel smears under pursuit
   regardless of pose correctness — unverifiable on a sheet, checkable
   in a runtime capture (high-speed/pursuit photography of 1x
   playback). Added to the runtime item as (h). Its retinal-slip
   numbers are not adopted (unverified); the check is.

No council claim survived re-verification that contradicts the
sub-verdicts below at their pre-registered scope; one presentation
duty, two disclosure duties, one scope split, and one runtime check
were adopted, and its Q5 pressure is credited for the EARLY anchoring
measurement.

## Decision — per-onset-class sub-verdicts (independent, pre-registered rule applied)

- **CONTROL (onset at arrival+1): PASS.** The banked seam reproduces
  exactly (machine-equal rows, native read matches). The regression
  anchor holds; nothing banked weakened.
- **LATE (onset walk tick 9, REM 4): PASS — the direct cut is banked as
  the answer for this class.** Onset cut 22.91/28.27 reads as onset
  beside a real walk cut; arrival lands inside the windup hold (the
  base glides to a stop under the held coil, +2,+1,+1 — visually a
  cushioned stop); from the release on, geometry and reads are
  byte/position-identical to CONTROL (full anchoring, banked recoil).
- **MID (onset walk tick 5, REM 8): PASS, with the shallow-late
  anchoring disclosed.** Onset and windup read as in LATE (larger
  drifts, +4,+3,+4, still coil-creep); the release fires at +12 and the
  strike crosses the arc line only on its final one (down) / two-ish
  (right) active ticks, 0–3px inside, coinciding with arrival — the
  destination read exists but arrives late and shallow. Banked as a
  PASS at sheet scope with the crossing profile recorded; the
  at-speed binding of a last-tick crossing joins the runtime item.
- **EARLY (onset walk tick 2, REM 11): FAIL — banked as an
  ENGINE-TERRITORY finding, not an asset defect.** Rubric line 2 fails:
  the strike's v4-banked anchoring read is absent — on every active
  tick the leading edge stays 3–13px (down) / 2–12px (right) short of
  the arc-tile grid line, so the gaped lunge never visually reaches the
  tile its hit lands on (the committed-tile lag made visible; the pose,
  salience, and onset cut all read, which makes the missing destination
  conspicuous rather than mitigating). Per the pre-registered routing:
  the failing variable is INPUT TIMING, not pixels — no rescue frame,
  no smear, no tick change. Recommendation recorded for the future
  runtime-replay integration design (a game-two decision for AFTER the
  cycle closes, never implemented from here): gate or buffer mid-walk
  attack onset so the active window cannot fire this far from arrival —
  the measured breakpoints are REM ≤ 4 (clean anchoring, LATE) and
  REM 8 (marginal, MID); deferring `start_attack` while REM > 8 costs
  at most ~5 ticks (~83 ms) of input latency at the EARLY boundary,
  deferring to REM ≤ 4 costs at most ~7 ticks (~117 ms) from the
  earliest onsets — the feel trade is runtime territory. An engine-side
  alternative (accelerating tween completion on onset) is named for
  completeness; it changes sim identity and is not this repo's call.

What this sprint does **not** claim: temporal fusion or strobe behavior
of any one-tick cut at 60 tps, teleport-vs-attack reads of the +13/+12
releases under motion, sample-hold display behavior, recovery-walk pose
priority (drawn per the declared model only), or any runtime
integration — those are runtime replay-capture questions, blocked by
the one-way boundary. The x0 banking-reversal condition at the head of
the consolidated runtime item is untouched; no tick was consumed; the
6-tick r0-hold floor stands.

Status per contract: **measurement banked; selectable candidates
unchanged.** No integration — v18 is open in game-two; the one-way
boundary and every runtime-integration stop condition in
`docs/asset-contract.md` hold. The lane-B grammar remains 16 creature
frames, byte-frozen; the walk→attack seam class is now measured, with
three of four onset classes reading at sheet scope and the fourth
routed to engine territory.

## Findings for any future integration design (the FULL v7 standing list carried unmodified, extended)

- **Moving-recovery pose priority** (carried, v5–v7): walking is legal
  during recovery; walk-vs-r0 priority undecided — x0 adds the
  recovery-tick-8 sub-case. **v8 extension:** EARLY's measured overlap
  (s0/r0 drawn on a still-moving base at +1px/tick for recovery ticks
  1–3, conditional on the declared drawing model) is the first
  concrete rendering of the sub-case this priority decision governs.
- **Exhaust-tail invisibility** (carried, v5): ~18 post-recovery locked
  ticks remain unmarkable asset-side.
- **Runtime temporal confirmation (single consolidated item, extended —
  the x0 banking-reversal condition stays at its head):** replay
  capture should verify (a) **the diminished ready-again beat still
  reads as the unlock cue at 60 tps and the r0→x0 step does not fuse
  into the held r0 span** (v7, reversal condition: if it fails, REVERSE
  the x0 banking); (b) x0 at 16.7 ms is not misread as an s0-variant;
  (c) the r0 hold reads as settle at 100.0 ms; (d) the carried v6
  checks — w0/s0 strobe/fusion, w0 coil-onset binding, 66.7 ms a0 hold;
  (e) the k0→s0 −6px return reads as recoil; (f) the ACC flash-accent
  under live flicker (v4). **v8 additions:** (g) the mid-walk onset
  cuts (22.91/28.27 — the v6 bridge-magnitude band) fuse rather than
  strobe at 16.7 ms, and the MID/LATE retreat-cue inversion (+1px
  forward under the gather pose) does not misread under motion; (h) the
  enlarged mid-walk release displacements (+13/+12, up to 3.25× max
  walk speed) read as attack rather than teleport, including the
  sample-hold display smear check (council Q6: pursuit/high-speed
  capture of native 1x playback); (i) MID's last-tick shallow arc-line
  crossing binds the hit to its tile at speed.
- **Onset-anchoring geometry (NEW, the v8 headline):** the strike's
  destination-crossing read requires the base near arrival — measured
  breakpoints REM ≤ 4 clean, REM 8 marginal (last-tick, 0–3px), REM 11
  absent (0/4 ticks, both facings). Any integration design that keeps
  unguarded mid-walk onset must either accept an unanchored strike
  class or add input buffering/gating at an engine-chosen threshold;
  the EARLY sub-verdict above records the recommendation. The v4
  EXP-row remedy attribution holds: the failing variable is timing
  (engine-side), never the pose.
- **Onset retreat-cue inversion (NEW):** mid-walk onsets surrender
  CONTROL's −3px pullback (EARLY −2, MID/LATE +1 forward); the pose
  alone carries onset per the v2-subthreshold and v4 pose-is-the-signal
  precedents at sheet scope. A future facing set or enemy reuse should
  not assume the position cue exists off-arrival.
- **Unmeasured onset sub-classes (recorded, not measured):** onset on
  the step-initiation tick (step+attack same controller tick, REM 13),
  onset ON the arrival tick (REM 0 at t14 — one pre-onset pixel
  different from CONTROL), and cross-facing onset (facing change +
  attack mid-tween — reachable at the pin via `face(dir)` before
  `start_attack`).
- **r0-hold floor** (carried, v7, tick-aligned): 6 held r0 ticks is the
  FLOOR; no future in-between may consume another recovery tick without
  runtime replay evidence first. Untouched by this sprint (zero tick
  changes, machine-asserted).
- **Facing-mirror handedness** (carried, v5–v7): unchanged; the seam
  classes add no new translation entries (zero new frames).
- **Enemy-side reuse** (carried, v4–v7): divided-attention
  re-verification extends to the onset classes — on an enemy body a
  mid-walk onset is a dodge prompt firing off-arrival, where the
  missing retreat cue and (for early onsets) missing anchoring may bind
  differently.
- **Bridge-lean asymmetry** (carried, v6/v7): unchanged; no new bridges.
- **Translation-registry discipline** (carried, v7): unchanged; no new
  states or bridges — the registry gained no entries this sprint.

## Next hypothesis (single, for a future bounded sprint — not started)

The along-facing walk→attack onset class is now measured; the
runtime-replay-capture integration design remains the highest-value
next step (carried from v7 — it now holds three sprints of temporal
questions plus the v8 additions (g)–(i), and it is gated on the game
cycle closing). If a calibration sprint happens first, the one
pose-only seam class still unmeasured at asset level is **cross-facing
onset** — attack input with a facing change during the tween
(down-walk→right-attack and right-walk→down-attack are both composable
from banked frames): the onset cut becomes fN(facing A)→w0(facing B),
a simultaneous state-and-facing swap whose magnitude the cross-facing
identity ceiling (44.44) was banked to bound. Same protocol: zero new
frames, lane sheet, jump tables, per-class sub-verdicts.

## Stop

Sprint 8 stops here: one reviewed seam sheet, one banked verdict, two
non-blocking APNG aids, one metrics JSON, ZERO new creature frames,
zero new exports, zero tick changes, two mechanical identity re-pin
commits plus two bounded change sets. No smear frames, no rescue edits,
no cross-facing measurement, no recovery-walk adjudication, no
8-direction set, no terrain, no enemies, no pack, no game-two changes.
The runtime-replay integration design stays parked until the game cycle
closes.
