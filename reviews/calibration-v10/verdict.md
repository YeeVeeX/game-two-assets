# Calibration v10 — pure-turn lane verdict (walk-A→walk-B mid-tween, no attack)

Reviewed artifacts:

- `reviews/calibration-v10/turn-sheet.png` — SHA-256
  `af2b7b7b5c55b678e7f2124594e98d56d30c40e63d462ab306d9d28d7748b771`,
  regenerated byte-identically (in-process double-build in
  `turn_seam_metrics` plus a full CLI re-run compared with `cmp`).
- `reviews/calibration-v10/turn-metrics.json` — SHA-256
  `8a98474a8da4e004875fdbbc17e5c698e2b1e141adc28eb84bea8aff52e9125c`,
  re-run equal under identical flags.
- Viewing aids (non-blocking): `turn-lanes-dr.apng` (`4c553750…`) and
  `turn-lanes-rd.apng` (`b580cade…`) — the four turn lanes side by side,
  30 frames t00..t29, exact 1/60 s per-frame delay via the banked APNG
  encoder, 0.5 s final hold, 4x nearest-neighbor; byte-identical on
  regeneration and equal to committed bytes (machine-checked; the DEGEN
  lanes' motion is the banked v1 walk, animated in the banked v1
  previews).
- **No release, no new pixels:** this sprint composed banked bytes only.
  All 26 banked export pins across the seven releases (v0–v3, v5–v7)
  byte-verified; `exports/` contains exactly the pinned files and no
  `calibration-v9`/`calibration-v10` directory; the `make_release.py`
  registry and `tools/export_assets.py` are untouched (machine-enforced).
- Baseline: one mechanical identity re-pin this sprint (step 0,
  `5fd7efd`): game HEAD `7e9b252f`→`fd006f91`, all five pinned hashes
  LF-identical, `attack_timing` re-verified 5/4/8/13 via
  `/kits/striker/...`; the stale `game_branch` field corrected
  `junior-tibia`→`main` in the same commit (the branch was merged away
  upstream; the field is provenance-only, not gate-checked — verified
  against local branches and `git ls-remote`). The v10-specific
  reachability facts verified at the pin: `face` L142–144 unconditional
  on the move path (controllers.rb L51–52 face-before-step, L56);
  mid-tween steps refused (grid_walker.rb L36); tween facing-blind, no
  snap (L90–97); the renderer reads `c.facing` LIVE at every draw
  (renderer.rb L520–531) and walk carries zero draw offset (L536–544);
  world tick order `tick_body`-then-controllers (world.rb L686–698)
  makes the B step commit ON the arrival tick when B is held. The
  pre-registered unreachability pivot branch did NOT trigger. Live gate
  exit 0 at step 0 and immediately before banking.
- Vision-critical judgment: session model `us.anthropic.claude-fable-5`
  (verified from `PI_MODEL`, not self-report), on the committed sheet
  plus deterministic crops of the same bytes (ephemeral diagnostics;
  native 1x lane rows decide, both zones, both pairs; 3x turn zooms, 2x
  wrap strips, the rendered CONTEXT row, and 4x/2x crops for diagnosis).
  Council seat cross-vendor (Kimi K2.5), one consolidated adversarial
  verdict, 4,271 tokens of the 8k cap (1,906 in / 2,365 out, single
  call, no truncation), response redirected to a file and read as UTF-8.

Question under test (the banked v9 next-hypothesis and council Q2's
explicit ask): does a bare mid-tween facing swap read as a TURN at 1x —
the rendered yardstick every v9 cross-facing judgment leaned on as
byte-pair calibration only, and the premise under the buffering
recommendation's "render the turn first" clause? Two pairs (DR down→
right, RD right→down) × four turn ticks (the v8/v9 onset ticks reused),
plus uncut two-step DEGEN lanes; zero new frames; 8 per-class-per-pair
sub-verdicts + the degenerate gate; FAIL routes to an ENGINE-TERRITORY
finding, never a rescue frame.

## Accuracy — all-must-pass (the pre-registered INTEGRITY bars)

| Item | Verdict | Evidence |
|---|---|---|
| Zero new exports; 26/26 banked pins byte-verified; no v9/v10 exports dirs; exporter/registry untouched | PASS | `turn_seam_metrics --check` export bar |
| Jump tables complete: t01→t02 .. t20→t21, pose Δ% + (dA,dB) + window (dx,dy) + exact integer squared per consecutive pair, 20 rows × 10 lanes | PASS | committed metrics |
| Anchor map (the risk-2 discipline): contexts == banked v9 `context_deltas` on all four fields; frame-identical cuts == their own context; f3 == idle byte-copy both facings; wrap/restart/boundary rows == banked v1 `motion-metrics` pairs; cross-lane pose-pair consistency; DEGEN prefix == CONTROL prefix row-for-row | PASS | 0 failures across all six anchor groups; every equality exact — no near-misses |
| Tick math exact: walk mapping f0×4/f1×3/f2×3/f3×3; facing A before the turn tick, B from it; commit ticks draw the standing pose; B commits t14 (moving) / t15 (CONTROL), first B advance t15/t16; REM 11/8/4/0; tween deltas 1,1,2,3,4,3,4,3,4,3,2,1,1 per axis; poses restricted to idle/f0..f3; no tick added, consumed, or reallocated | PASS | `--check`, 0 failures |
| In-window by construction: [2,2,29,29] contract bounds inside lane windows (turn 2×2 tiles, DEGEN 3-tile axis), turn crops, wrap windows — proven from the plan; T0/T1/T2 + grid lines in every turn window | PASS | `bounds_failures: []` |
| Byte-determinism (sheet, metrics, both APNGs): in-process double-build + CLI re-run `cmp` vs committed bytes | PASS | SHAs above; `deterministic: true`, `committed_matches: true`, re-run equal |
| Composition purity: every creature cell = banked export bytes at the computed integer draw vector | PASS | 524 cells, dual verification (region reconstruction + direct export-byte equality), 0 failures |
| Banked artifacts stand: v0–v9 sheets/metrics regenerate byte-identical; full suite green; coverage ≥ 80 | PASS | 426 tests OK (4 skips = pre-bank artifact guards, green after banking); gauntlet full run |
| Category law: every numeric bar within one facing-category; no cross-category dominance bar exists in the tool | PASS | M1 compares cross-facing cuts to the cross-facing band; same-facing rows to banked walk-cycle values; structural |
| Pass bars pre-registered before artifacts existed; INTEGRITY/MEASUREMENT split fixed in the rationale first | PASS | rationale + toolchain committed (`a8a296e`) before any artifact was generated |
| No game-two changes | PASS | read-only `git show` against `../game-two` only |

**Accuracy: 11/11 integrity bars.** M1 (the only numeric MEASUREMENT
bar) **passed 8/8** — every turn cut sits inside the free-turn context
band; no machine red fired this sprint. The failing sub-verdicts below
fail on pre-registered PERCEPTUAL lines, adjudicated at sheet scope
exactly as the rationale fixed them.

## Measured evidence (conventions inline, per the banked evidence-table law)

Pose Δ% = 100·XOR/union on export bytes in SPRITE-LOCAL space — the
draw position never enters the XOR; displacement lives only in (dA,dB).
A = the first walk axis (DR +y, RD +x); B = the direction turned to.
REM = 13 − (turn − 1). The strafe segment (B-facing frames displacing
along A) spans the turn tick through arrival: REM + 1 drawn ticks.

| Turn-cut table (pose Δ% / (dA,dB)) | EARLY t03 | MID t06 | LATE t10 | CONTROL t15 |
|---|---|---|---|---|
| DR cut fN@down→fM@right | f0→f0 **53.64** /(1,0) | f0→f1 **50.00** /(4,0) | f2→f2 **36.45** /(4,0) | f3→idle **44.44** /(0,0) |
| RD cut fN@right→fM@down | f0→f0 **53.64** /(1,0) | f0→f1 **51.61** /(4,0) | f2→f2 **36.45** /(4,0) | f3→idle **44.44** /(0,0) |
| free-turn context (banked band [36.45, 53.64]) | == f0 53.64 exactly | inside band, below f0 | == f2 36.45 exactly | == f3/idle 44.44 exactly |

EARLY/LATE/CONTROL cuts are frame-identical to their context pair BY
the banked mapping — those equalities are INTEGRITY self-checks of the
derivation, not findings. **The sprint's novel pose numbers: MID's
compound (turn + frame-advance) cuts, 50.00 DR / 51.61 RD — inside the
band, BELOW the worst pure swap (f0's 53.64), above f1's 45.00.** Scope
fixed per council Q4: this containment is a pose-MAGNITUDE fact; it
does not by itself establish that a compound cut reads like a pure
swap — the sheet-scope read below and the runtime item carry that.

Wrap/restart rows (machine-anchored to the banked v1 walk cycle by the
f3=idle byte-copy law): moving lanes f3@B→f0@B at t14→t15 = 10.29 (DR,
right) / 15.87 (RD, down), displacement pivot (dA,dB) = (0,1);
CONTROL f3@A→idle@B 44.44 (0,0) then idle@B→f0@B 10.29/15.87 (0,1);
DEGEN wrap f3→f0 along-axis (1,0) at the same banked values. All
same-facing boundary rows equal their banked v1 numbers (down 19.64 /
19.00 / 15.87; right 22.09 / 22.09 / 10.29); max same-facing row 22.09
— the walk system's own loudest step. Max squared displacement
anywhere = 16 (the peak-velocity tick); no offset exists in any lane.

Body-tile binding during the strafe (committed tile is T1 from t01;
majority by overlap px, ties → T0; identical both pairs — margins per
council Q3's duty):

| Lane | strafe ticks | T0-majority ticks | overlap at the last T0 tick | first T1-majority tick |
|---|---|---|---|---|
| EARLY | 12 (≈200 ms; REM 11 advances ≈183 ms) | **5 consecutive** (t03–t07) | t07: T0 15px / T1 10px | t08 (11/14) |
| MID | 9 | 2 (t06–t07) | t07: 15/10 | t08 (11/14) |
| LATE | 5 | 0 | — | t10 (3/21 at entry) |
| CONTROL | 0 (stand beat instead) | 0 | — | — |

Strafe gait content (the L2 evidence): EARLY carries the full cycle
under perpendicular travel — two ACTIVE gait boundaries (f0→f1 t05→t06,
f1→f2 t08→t09) plus the settle (f2→f3, where f3 IS the standing
byte-copy); MID carries one active boundary (f1→f2 t08→t09) plus the
settle; LATE carries settle-only (f2→f3 at t11→t12, then standing bytes
easing 2,1,1 px into the tile); CONTROL strafes nothing.

## Rubric line by line (pre-registered; judged at native 1x on the committed sheet, both zones, both pairs; sheet-scope — never temporal fusion at 60 tps)

1. **Turn-read (the swap under displacement). PASS all 8.** At 1x, in
   the 3x zooms, and against the rendered CONTEXT row, every cut reads
   as the SAME creature rotating while its slide continues: the frozen
   5-color ramp, body mass, eye accent, and feet row carry across the
   cut; the head re-forms front-skull↔profile-snout — one creature
   turning, never a swap error. Precision fixed per council Q1: this
   PASS rests on the NATIVE READ of the rendered, DISPLACED cuts — the
   band equalities are derivation integrity, and no byte-pair number is
   treated as proof of perceptual equivalence under displacement (the
   v9 category-error lesson, honored in the routing). MID's compound
   cut also reads as a turn at sheet scope; its at-speed read is
   routed to the runtime item (new sub-item (l) below).
2. **Strafe legibility. FAIL for EARLY and MID, both pairs — the named
   broken-slide condition; PASS for LATE and CONTROL.** The
   pre-registered FAIL names "the leg cycle fights the displacement
   direction (moonwalk/dragged read)". The committed frames CONTAIN
   that condition for EARLY and MID: the gait visibly advances (EARLY
   two active boundaries across 12 ticks ≈ 200 ms; MID one across 9
   ticks ≈ 150 ms, landing on the peak-velocity ticks) while the body
   travels perpendicular to its facing. Adjudicated FAIL at sheet scope
   per council Q2 (adopted after re-verification against the committed
   strips): deferring a condition already present in the static
   evidence to "runtime" would downgrade a pre-registered bar — the
   Rule-6 move this series exists to prevent. The graded story is
   recorded verbatim: at 1x the read is carried mostly by the smooth
   tween (the feet are 2–3 px; the alternation is subtle; nothing reads
   as knockback or ice), so this is the mild end of the named failure —
   but the bar names the condition, the condition is on the sheet, and
   the bar fires. LATE passes on structural difference, not grading:
   its strafe is settle-only (f2→f3 = the standing byte-copy easing
   2,1,1 px into the tile center) — a turned creature decelerating into
   its tile, the momentum-carry read the bar asks for. CONTROL's stand
   beat (1 tick, idle@B at T1 center) reads as a deliberate pivot, not
   a freeze.
3. **Arrival restart. PASS all 8.** The f3@B→f0@B wrap plus the first
   B advance reads as a new step taken in the new direction — the wrap
   is the smallest cut in the walk cycle (10.29/15.87,
   machine-anchored to the banked v1 wrap), the displacement pivots
   cleanly (0,1), and the 2x wrap strips show an immediate step-off
   with no pose rewind. CONTROL's stand-then-step (44.44 pivot beat,
   then the same 10.29/15.87 restart) reads as arrive→turn→go. DEGEN's
   wrap is invisible as a seam — continuous walking (the banked read).
4. **Tile binding during the strafe. FAIL for EARLY, both pairs (the
   pre-registered expected red, confirmed); PASS for MID/LATE/CONTROL.**
   EARLY: five consecutive T0-majority strafe ticks (t03–t07, margins
   24/0 → 15/10) with the facing no longer explaining the offset — at
   1x the B-facing creature reads as standing IN the tile the engine
   has already left, ≥ the pre-registered 3-tick span; the v9 EARLY
   wrong-tile precedent transposed from the arc to the body. MID sits
   at 2 ticks (t06–t07, 18/7 → 15/10), below the span, and reads as
   transit at 1x; LATE enters T1-majority on its first strafe tick;
   CONTROL stands at T1 center throughout.
5. **Degenerate gate. PASS sheet-wide.** DEGEN lanes machine-equal
   their anchors (prefix == CONTROL rows; wrap == banked v1 numbers)
   and read as the banked v1-class continuous walk; identity law holds
   across all 524 dual-verified cells (byte-frozen frames at computed
   vectors, frozen ramp, feet/anchor law inside the moving cells,
   nearest-neighbor everywhere).

**Presentation scores (accuracy scored separately above):**

- **DR section: 8.5/10.** The 2×2-tile windows make the body-tile
  question a single glance — the EARLY wrong-tile span is visible
  against the drawn grid junction exactly where the binding table says
  to look; the 21-column rows carry the whole story (commit → walk →
  turn → strafe → wrap → B-walk) in one eye movement.
- **RD section: 8.5/10.** Mirrored geometry reads identically; the
  front-facing strafe (RD) is the more legible of the two at 1x (the
  down-facing sprite's symmetry makes the perpendicular drift read
  quieter than DR's profile).
- **Sheet as artifact: 8/10.** The TURN 3X zooms isolate the swap
  beside its displacement; the WRAP 2X strips make the restart
  judgment trivial; the rendered CONTEXT row (the thing v9 lacked)
  anchors every cut against the stationary yardstick. Cost of the
  density: the 3020×2868 canvas needs the crops for careful native
  reading — accepted, same trade as v9.

## Structured critique and cross-vendor review (Kimi K2.5, adversarial)

One consolidated verdict (single call, 4,271 of 8k tokens); every
numeric claim recomputed against pixels/numbers before adoption; every
REFUTED re-verified against the primary evidence. Reconciled
claim-by-claim:

1. **Q1 (L1 routing) UNCERTAIN — precision ADOPTED, routing kept.**
   The council could not verify that a frame-identical byte pair
   proves perceptual equivalence under displacement. Correct — and
   never claimed: the rationale pre-registered the displaced cuts as
   exactly the content the stationary context row cannot carry. L1's
   wording above now states explicitly that the PASS rests on the
   native read of the rendered displaced cuts; band equalities are
   integrity anchors only.
2. **Q2 (L2 moonwalk) REFUTED — ADOPTED after re-verification; two
   sub-verdict flips.** The council's charge: the static evidence
   already contains the named FAIL (gait cycling perpendicular to
   travel), and routing it to the runtime item was special pleading —
   L1/L3/L4 are judged from the same static evidence. Re-verified
   against the committed strips: EARLY carries two active gait
   boundaries, MID one, under perpendicular travel — the condition IS
   present; provisional PASSes for EARLY/MID were re-adjudicated to
   FAIL before banking (this document is the first banked
   adjudication). LATE/CONTROL pass on structural difference
   (settle-only strafe / stand beat) — a per-class distinction fixed by
   the evidence, not a post-hoc soften. The operational trigger is now
   explicit: an ACTIVE gait boundary (f0→f1 or f1→f2) under
   perpendicular travel fails; settle-only content passes.
3. **Q3 (L4 split) CONFIRMED — margins duty ADOPTED.** The ≥3-tick
   span was pre-registered in the rationale before artifacts; the
   5-vs-2 split is arithmetic. The council's margin note is folded into
   the binding table above (overlap px at every boundary tick).
4. **Q4 (MID compound inference) REFUTED — scope ADOPTED.** Band
   containment (50.00/51.61 ≤ 53.64) is a pose-magnitude fact and is
   now claimed as nothing more; whether a turn+frame-advance compound
   reads as a different perceptual category at speed is unmeasured —
   added to the runtime item as (l). MID's sub-verdict fails on L2
   regardless, so no routing rested on the refuted inference.
5. **Q5 (CONTROL model) CONFIRMED.** No unverified engine dependency:
   the commit-tick standing pose extends the banked t01-idle precedent
   under the declared model; every engine-side step in the chain is
   cited at the pin.
6. **Q6 (unthought risks) — one adopted as a finding, two refuted with
   the mechanism, one folded.** (a) Facing-notch compositing: REFUTED
   for the sheets — no notch exists in the composition (purity verifies
   every non-floor pixel equals export bytes; the notch is the
   RUNTIME's primitive-body visual, and the sprites carry facing in the
   art). But the kernel is banked as a finding: at the pin the LIVE
   game already renders this class — the notch reads live facing
   (L520–531) while the tween is facing-blind, so a mid-tween re-face
   already shows a B-pointing notch sliding along A today; the sprite
   set makes the class louder (gait), not new. (b) Rendered-output
   equivalence: REFUTED — integer draw vectors only, nearest-neighbor
   scaling, dual-verified purity (524/524); there is no subpixel or
   palette path for divergence. (c) Velocity-profile hitches at gait
   boundaries: recorded as an observation — the delta sequence is the
   engine's pinned smoothstep, not an asset variable; any hitch is
   engine-territory by construction. (d) "Subordinate is not an
   operational definition": folded into the Q2 adoption (the active-
   boundary trigger).

Net: one council REFUTED changed the decision (Q2 — two lanes flipped
to FAIL before banking); one narrowed a claim's scope (Q4); one added
reporting duty (Q3); one added a finding (Q6a). No council claim
survived re-verification that contradicts the decision below.

## Decision — 8 per-class-per-pair sub-verdicts + the degenerate gate (pre-registered rule applied)

The rule (fixed before artifacts): a class+pair PASSES only if rubric
lines 1–4 pass for it and line 5 holds sheet-wide. Line 5 holds.

- **DR/EARLY and RD/EARLY (turn at REM 11): FAIL on two independent
  grounds — banked as ENGINE-TERRITORY findings.** (a) L2: the full
  gait cycle (two active boundaries) runs perpendicular to travel for
  12 ticks ≈ 200 ms — the named moonwalk condition, present in the
  committed frames. (b) L4: the B-facing body claims the departed tile
  T0 for 5 consecutive ticks (margins 24/0 → 15/10) — the v9
  wrong-tile precedent transposed to the body. The failing variable is
  INPUT TIMING (a turn at REM 11), never the pose.
- **DR/MID and RD/MID (REM 8): FAIL on L2 alone.** One active gait
  boundary rides the peak-velocity ticks perpendicular to travel
  (≈150 ms strafe); binding passes (2 ticks < the span), the cut reads
  as a turn, the restart is clean. The mildest failing class — and the
  compound cut's magnitude (50.00/51.61) stays inside the free-turn
  band.
- **DR/LATE and RD/LATE (REM 4): PASS.** The cut reads as a turn
  mid-crossing; the strafe is settle-only deceleration into the tile;
  the restart is clean; binding enters T1-majority immediately. The
  mid-tween turn at REM ≤ 4 is banked as READABLE with zero engine
  changes.
- **DR/CONTROL and RD/CONTROL (REM 0): PASS.** Arrive → one-tick pivot
  beat → step off. The ordinary tile-boundary direction change is
  clean at 1x, both zones — the class's yardstick member, now rendered
  and banked.
- **Degenerate gate: PASS.** Machine-equal to every anchor; reads as
  the banked continuous walk.

**Net: 4/8 PASS (LATE, CONTROL × both pairs), 4/8 FAIL (EARLY, MID ×
both pairs), all FAILs engine-territory.** The headline answer to the
sprint question: **the bare mid-tween facing swap DOES read as a turn
at 1x — L1 passed 8/8, settling the premise under v9's L1 PASSes and
the buffering recommendation's render-the-turn-first clause. What
fails is not the CUT but the SEGMENT the turn creates: a gait cycling
perpendicular to residual travel (REM ≥ 8) and, at REM 11, a body
visually parked in a tile the engine has left.**

**Recommendation recorded for the future runtime-replay integration
design (game-two decisions for AFTER the cycle closes; never
implemented from here, never compensated with pixels):**

- The v8/v9 **facing-aware onset buffering** clause is now grounded:
  rendering the turn first is confirmed viable — the turn cut itself
  reads cleanly at every REM measured (L1 8/8). Buffering the ATTACK
  behind the turn remains the standing recommendation and nothing here
  weakens it.
- **NEW, the v10 headline: strafe-segment frame selection.** When
  facing ⊥ travel (a re-face during residual tween), cycling the gait
  is the failing surface — an integration design should select the
  standing/settle frame (f3/idle class) for strafe ticks instead of
  advancing the walk cycle, or lock facing during the tween (the v9
  alternative, ~217 ms worst-case turn latency, feel cost already
  specified). Frame selection is runtime code at integration time —
  engine territory, zero pixel changes.
- **EARLY additionally inherits the REM-gate class** (v8/v9): at
  REM 11 the turn also mis-binds the BODY tile for ~83 ms — a turn
  buffered to REM ≤ 4 (or to the boundary) avoids both failing
  surfaces by construction; LATE/CONTROL prove those classes clean.

What this sprint does **not** claim: temporal fusion or strobe of the
turn cut at 60 tps; the at-speed read of the strafe gait (the
sheet-scope FAIL names the condition's presence, not its 60 tps
severity); whether MID's compound cut reads as a different perceptual
category at speed (runtime item (l), council Q4); the live game's
notch-strafe legibility (code-derived, capture-blocked); any runtime
integration. The one-way boundary held throughout: read-only
`git show` against `../game-two`.

## Findings for any future integration design (the v8/v9 standing list carried unmodified, extended)

- **Moving-recovery pose priority** (carried v5–v9): unchanged; no
  attack in this sprint.
- **Exhaust-tail invisibility** (carried, v5): unchanged.
- **Runtime temporal confirmation (single consolidated item, extended —
  the x0 banking-reversal condition stays at its head):** items
  (a)–(k) carried unmodified from v9. **v10 additions:** (l) whether
  MID's turn+frame-advance compound cut (50.00/51.61 — inside the
  free-turn band) fuses into one turn event at 60 tps or reads as a
  distinct pivot-stride/stutter category (council Q4's unmeasured
  half); (m) the at-speed severity of the strafe-gait moonwalk (the
  sheet-scope L2 FAIL for EARLY/MID) and of EARLY's 5-tick body
  mis-bind — replay capture should measure both against the same
  pre-registered conditions.
- **Onset-anchoring geometry** (v8/v9 headline, carried): unchanged;
  v10 adds the BODY-side transposition — at REM 11 the failure axis
  includes the creature's own tile claim, not just the strike's.
- **Cross-facing onset-cut magnitude** (v9 headline, carried):
  unchanged; v10 adds the missing yardstick — the free-turn band is
  now RENDERED, and the pure-turn cut at every measured REM reads as a
  turn at 1x (the v9 L1 premise held).
- **Pure-turn lane (NEW, the v10 headline):** the turn cut is clean at
  every REM (L1 8/8; frame-identical cuts equal their contexts by
  construction; the MID compound 50.00/51.61 stays inside the band);
  the STRAFE SEGMENT is the failing surface — active gait boundaries
  under perpendicular travel fail legibility at REM ≥ 8 (both pairs),
  and REM 11 adds a 5-tick wrong-tile body bind (margins banked). The
  remedy space is engine-side frame selection (settle frame during
  strafe) or facing-lock; the turn-then-act ordering the v8/v9
  recommendations assume is validated.
- **Live-runtime notch strafe (NEW, code-derived):** at the pin the
  engine already renders this class — `draw_facing_notch` reads live
  facing while the tween is facing-blind, so a mid-tween re-face shows
  a B-pointing notch sliding along A in the current game; the sprite
  set amplifies (gait) rather than introduces the class. Unmeasured at
  runtime (capture parked); recorded so the integration design treats
  strafe rendering as a PRESENT surface, not a sprite-era novelty.
- **Bar-design lesson** (v9, carried): honored this sprint — every
  numeric bar within facing-category; M1 was the only numeric
  measurement bar and it compared cross-facing cuts to the
  cross-facing band. **v10 extension (council Q2's process kernel): a
  pre-registered perceptual FAIL condition that is structurally
  present in the committed frames must be adjudicated at sheet scope —
  "routed to runtime" is not an available disposition for conditions
  the static evidence already contains; only SEVERITY-at-speed may be
  routed.**
- **Onset retreat-cue inversion** (carried, v8/v9): unchanged; no
  offsets exist in pure-turn lanes (engine fact, verified).
- **Unmeasured turn sub-classes (recorded, not measured):** turn on
  the step-initiation tick; turn ON the arrival tick t14 (the seamless
  wrap with the facing swap riding the wrap cut — the one no-stand
  boundary turn not rendered here); multi-turn chatter (A→B→A
  mid-tween); 8-direction turns; facing drift during windup (carried
  from v9).
- **r0-hold floor** (carried, v7): untouched (no attack ticks
  anywhere; machine-asserted pose strip).
- **Facing-mirror handedness** (carried, v5–v9): unchanged; the
  context band numbers would all change under a mirrored set —
  re-verify then (carried clause).
- **Enemy-side reuse** (carried, v4–v9): the strafe-gait and
  wrong-tile-body findings transfer amplified to enemy bodies (a
  strafing enemy that reads as occupying the wrong tile mis-telegraphs
  dodges under divided attention).
- **Bridge-lean asymmetry / translation-registry discipline** (carried,
  v6–v8): unchanged; no new states or bridges — the registry gained no
  entries.

## Next hypothesis (single, for a future bounded sprint — not started)

The runtime-replay-capture integration design remains the
highest-value next step (carried v7→v10 — it now holds five sprints of
temporal questions, (a)–(m), and it is gated on the game cycle
closing). If a calibration sprint happens first, the sharpest
asset-level gap this sprint exposed is the **arrival-tick turn**
(B pressed exactly at t14): the one boundary-turn class not rendered —
the facing swap rides the f3→f0 wrap cut ON the commit tick (no stand
beat, no strafe), likely the cleanest legal turn the engine permits
and the natural CONTROL twin for the buffered-turn recommendation. It
is one lane per pair, zero new frames, the v10 toolchain reused as-is.

## Stop

Sprint 10 stops here: one reviewed pure-turn sheet, one banked verdict,
two non-blocking APNG aids, one metrics JSON, ZERO new creature frames,
zero new exports, zero tick changes, one mechanical identity re-pin
commit plus two bounded change sets. No smear frames, no rescue edits,
no bar softening (two provisional PASSes were flipped to FAIL by
adversarial review before banking — the reverse direction), no attack
lanes, no arrival-tick turn, no 8-direction set, no recovery-walk
adjudication, no terrain, no enemies, no game-two changes. The
runtime-replay integration design stays parked until the game cycle
closes; the audio thread stays parked with game-two-audio/owner.
