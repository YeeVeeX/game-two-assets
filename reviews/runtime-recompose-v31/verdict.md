# v31 — first RUNTIME recomposition: single-creature instrument readiness — verdict (judged)

**Answer first: PASS on all six bars — the E3a chain's last missing link
is closed the small, provable way. The intaken track 1's possessed
creature (name `striker`, kit `striker` — the kit gate passed) is
extracted under the pre-registered protocols and rendered as the FIRST
RUNTIME-EXP artifact set: span [420, 483] (the longest refusal-free
contiguous span and the only one containing a complete attack cycle —
w0@433 through x0@449, returning to idle), declared sub-window origin
[192, 352] at 160x128 (whole-TILE aligned, one-TILE margin), native
per-tick contact sheet + exact-1/60 s APNGs at native and 4x, all
double-build deterministic with committed sha256s and a live SLOW-tier
regeneration guard. The district palette is 100% anchored to the pinned
`data/zones/district.json` blob (five values, five citations, zero
invented) in a NEW additive manifest merged opt-in — the plain render
reference is byte-untouched and the banked `unmapped-zone` refusal stays
live. Mapping semantics untouched: 672/672 + 340/340 equivalence green,
draft-1 suite byte-identical, v1 suite untouched. ZERO adjudication —
every artifact answers ZERO register items and says so in pixels,
filenames, and manifest; the at-speed and declared-viewport watch items
are now artifact-VIEWABLE and stay NOT MEASURED. C6 stays OPEN.**

**Authorization (owner order, recorded verbatim per the v31 brief):**
"AUTHORIZATION: the v30 verdict pinned that rendering any RUNTIME track
'waits for its own owner brief' - THIS is that brief, owner-launched
(record this line in the verdict as the owner order)."

Review objects: the intaken bundle `20260826T175326Z_p1_42` (track 1
sha256 `dd68c8cb…44545d`, verified live at generation by
`verify_runtime_intake`); the pre-registered protocols
(`rationale.md`, commit A `1026dcc`, cells NULL); the consumer extension
(commit B `caa31f2`); the artifact set (commit C `5f69824`,
`runtime-exp-manifest.json` repo_commit_at_generation = `caa31f2…`).
Session model `us.anthropic.claude-fable-5` (verified from `PI_MODEL`).
Council: cross-vendor adversarial, brief 8,539 B inlining protocols,
machine outputs, palette citations, and disclosure strings verbatim;
DeepSeek V3.2 first pick returned 351 output tokens WITHOUT labeled
Q1–Q3 verdicts (incomplete coverage = the pre-registered fallback
trigger), Kimi K2.5 fallback returned full labeled verdicts; totals
2,292+351 + 2,215+2,034 = 6,892 tokens, within the 8k budget;
reconciliation below.

## The bars, judged

| # | Bar | Verdict |
|---|---|---|
| B1 | Protocol fidelity | PASS — subject derived mechanically (possessed 141/141, one stable holder, kit striker); candidate runs (420-483, 64) / (490-549, 60) / (556-560, 5) with typed refusal bursts 484-489 + 550-555 (`unmapped-tween-class`, tween_total 7); exactly ONE complete cycle exists (w0 433 → x0 449, idles after) so run 1 is the only qualifying span AND the longest run — selection forced by the pre-registered rule, zero post-hoc adjustments; window formula outputs [192,352] 160x128, tile-aligned with >= 1 TILE margin (derivation reproducible: the SLOW guard re-derives it from committed evidence every full gate). ONE typed deviation, mechanical-impossibility class: the pixel banner's pre-registered "NOT THE LIVE CAMERA" needs a V glyph the banked SEAM_FONT does not have (the banked v13 precedent line in the consumer already spells around the missing V) — drawn as "NOT THE GAME CAMERA"; the manifest prose carries the pre-registered wording exactly. Judged: wording deviation forced by a banked-font constraint, meaning preserved, disclosed here and machine-scanned clean |
| B2 | Regression | PASS — `--check` exit 0 after every edit batch (672 v10/v11 cells + 340 v9 records green throughout); `tests/test_track_recompose.py` diff +59/-0 (pure insertion; the 33 banked draft-1 tests byte-identical and green); `tests/test_track_v1.py` untouched; the four-manifest fan-out surfaced exactly as pre-registered and resolved by each manifest's OWN tool — per-manifest git diff = the consumer pin line (`c1a114b9…` → `19c63562…`) + `repo_commit_at_generation`, every banked artifact byte-identical (adoption-report.json rewritten with zero diff); full discover green at N=788 = 748 + 36 (`test_runtime_extract`) + 4 (`RuntimeArtifactGuards`), exactly the new tests (`Ran 788 tests in 4095.694s` / `OK` / EXIT:0, captured `v31_discover.log`, detached run) |
| B3 | Palette anchoring | PASS — five values, five citations to the pinned `data/zones/district.json` palette fields (floor/grid/wall/transition verbatim; motif ← `motif_rgb` per the banked zone_1/zone_2 convention); `manifests/render-reference.json` byte-untouched (empty diff); the banked v30 `unmapped-zone` test green and the refusal class unweakened (plus a new plain-reference refusal test); the palette file's anchoring block binds to CONTENT (`source_sha256_lf` = the runtime-baseline pin, test-asserted), so identity-only re-pins never invalidate it; `data/display.json` examined, zero values needed; sheet gutter/bg/label = banked presentation constants, named as convention; council Q2 CONFIRMED value-by-value |
| B4 | Determinism | PASS — in-process double build byte-identical at generation (manifest flag true; the CLI hard-fails otherwise); all four committed blobs re-hashed EXACT against the manifest pins from `git show HEAD:` (post-`-text`-routing proof; `/reviews/**/*.apng -text` added — APNGs were previously `text: auto`); the SLOW guard regenerated everything byte-identically from committed evidence (4/4 green, 69.3 s); `--check` gained the guard additively (pre-bank skip pattern; "(banked)" state green) |
| B5 | EXP boundary | PASS — the RUNTIME-EXP disclosure appears in pixels (sheet banner 2 lines + footer `ANSWERS ZERO REGISTER ITEMS`; APNG 2-line headers), in every artifact filename (`runtime-exp-`), and verbatim in the manifest and the decision capture; adjudication-language machine-scan over the artifact strings came back clean BEFORE council; council Q1 REFUTED the smell charge ("No quoted string claims or implies a register answer… 'CAPTURED STATE IS REAL' is accurate and immediately qualified"); zero SYNTHETIC labels on RUNTIME artifacts; zero register/status edits; frozen docs and surfaces byte-untouched; census 4 |
| B6 | HFO gate | PASS — accuracy and presentation judged separately per artifact (section below) |

## Findings (typed per the pre-registered classes)

- **f1 (protocol execution, DONE):** the whole pre-registered pipeline
  ran forward-only: intake gate LIVE at generation → subject law → kit
  gate → decision capture (banked, 141 entries, 129 mapped / 12 typed
  refusals) → span policy → window formula → extraction →
  double-build composition. The instrument shows a walk-in (f0…f3), the
  complete attack cycle, an 18-tick idle hold, and a second attack whose
  recovery the ENGINE cut short — real captured combat, drawn under the
  declared mapping.
- **f2 (mechanical deviation, typed, judged in B1):** pixel banner
  LIVE→GAME under the missing-V constraint; manifest prose unchanged;
  meaning preserved; the pre-registration meets the same banked-font
  limit the v13 banner met (its own comment line is the precedent).
- **f3 (route finding):** enumeration found ZERO byte pins of
  `render-reference.json` anywhere (no releases/ dir; all four
  historical sha256s grep clean) — the brief's Route A trigger — BUT the
  decisive pin site is a banked TEST:
  `test_unbanked_zone_refuses_typed` (v30) asserts zone `district`
  refuses against the PLAIN reference. Route A would flip a banked test
  red (violating this sprint's own additive-only law), so the Route B
  MECHANISM (new additive manifest + opt-in `load_zone_palette` merge,
  collision-refusing) was chosen. Council Q2: "the stricter reading, not
  a violation."
- **f4 (noted, council Q3/Q4 kernels adopted as documentation):** the
  span policy selects the longest CLEAN span — the knockback bursts
  (484-489, 550-555) and the interrupted recoveries of runs 2-3 are not
  in the APNGs. That selection is disclosed structurally (sheet labels
  T420..T483 against the manifest's window_frames 420..560 +
  `span_policy` verbatim in manifest and capture), and the UNBIASED
  record — every tick including refusals — is the banked
  `decisions-subject.json` beside the artifacts. Kimi's wording kernel
  (an explicit temporal-selection line in PIXELS, e.g. "EDITED TEMPORAL
  WINDOW") is recorded as a candidate improvement for the NEXT
  RUNTIME-EXP generation, owner-sequenced — the banked artifacts are not
  edited post-registration (B1's own law).
- **f5 (noted, council Q5 charges re-verified against primary bytes):**
  (i) Kimi "lunge offset unverified/unanchored" — REFUTED:
  `windup_px -3 / active_px +6` are render-reference values anchored to
  `renderer.rb lunge_offset` at the pin, re-verified at EVERY re-pin by
  pin_drift's `lunge_windup`/`lunge_active` needles
  (tools/pin_drift.py L264-271), byte-proven in the mapping by the
  340-record v9 plan equivalence and the banked offset unit test
  ([-3]x5 + [6]x4 + [0]x8). The window formula consumes exactly those
  mapped offsets — a DECLARED-model property, disclosed ("frame
  selection is the proposal's"). (ii) DeepSeek "opt-in merge = covert
  regression channel" — REFUTED on the shipped bytes: the fast tier
  composes over the district palette at EVERY commit
  (`test_runtime_extract` pipeline fixtures, zone `district`), and the
  SLOW guard regenerates the committed artifacts at every full gate; a
  palette regression turns bytes, and bytes are pinned. Adopted sliver,
  named as DESIGN: the district palette participates in ZERO other
  surfaces (feedback metrics, sheets, demo all read the plain
  reference) — banked-zones-only law everywhere except the explicit
  RUNTIME-EXP path, until an owner decision says otherwise.
- **f6 (new pin site, pre-registered consequence):** the v31 manifest
  pins the consumer's sha256, so a future `track_recompose.py` edit fans
  out to FIVE manifests (v13/v14/v15/v16 + v31), each regenerated by its
  own tool under the same lawfulness condition. v32 inherits this.

## Pre-registered mechanical consequence, resolved as registered

Editing the consumer moved its sha256; all four banked manifests
regenerated by their own tools (`--make-demo`, `--make-audit`,
`--make-artifacts` x2); per-manifest diff = exactly

```
-    "tools/track_recompose.py": "c1a114b981a0...0379c765"
+    "tools/track_recompose.py": "19c63562a3b8...464009be"
-  "repo_commit_at_generation": "474c7d1377d4229e9aa010e8c6695705777de2b2",
+  "repo_commit_at_generation": "1026dcccfd820e65150884a9901852d9c441ed69",
```

with every banked artifact byte-identical (v13 demo track/sheet/APNG pins
unchanged inside; v14's 12, v15's 28, v16's 9 artifacts reproduced with
zero diffs). Fan-out beyond the four = none.

## Structured critique and cross-vendor review

DeepSeek V3.2 (first pick) returned an incomplete review — 351 output
tokens, no labeled Q1/Q2/Q3 verdicts (its unlabeled REFUTED quotes the
banner-deviation lines; its Q5 kernel is reconciled in f5.ii) — which is
the fallback trigger; Kimi K2.5 (fallback) returned full labeled
verdicts: **Q1 REFUTED (no adjudication smell), Q2 CONFIRMED (anchoring
lawful, route switch "the stricter reading"), Q3 UNCERTAIN (bias exists;
disclosure adequacy a judgment call), Q4 REFUTED (strings prevent direct
misreading; residual temporal wording kernel), Q5 CONFIRMED (lunge
anchoring charge).** Per the v12–v30 precedent every adverse or
uncertain claim was re-verified against primary bytes before folding:
Q3/Q4 kernels adopted as f4 (documentation + next-generation candidate,
no post-hoc artifact edit); both Q5 charges REFUTED with the pinned
evidence named in f5; no bar moved post-registration; every adoption
made a claim smaller or more explicit; all adoptions folded before this
commit.

## HFO gate (accuracy vs presentation, separately)

- **Disclosure strings (pixels + manifest):** Accuracy — every clause
  states a machine-verifiable fact (state from a verified intaken
  bundle; frame selection = the version-pinned mapping id; one creature;
  window ≠ camera; zero register answers) and the one qualitative word
  ("real") is immediately qualified by the proposal clause (council Q1
  concurs); PASS. Presentation — the pixel banner leads with the class
  (RUNTIME EXP), states the boundary in two short lines, and the footer
  restates the zero-answers law where the eye ends; the manifest carries
  the full prose verbatim; PASS.
- **runtime-exp-manifest.json (machine+owner audience):** Accuracy —
  every hash recomputed this session over named bytes (committed blobs
  re-hashed EXACT from `git show HEAD:`); span/window values equal the
  guard-reproduced derivation; the last-frame 0.5 s hold is disclosed
  rather than left implicit; PASS. Presentation — flat sorted-key JSON,
  formulas restated with their registration pointer, no unlabeled
  precision; PASS.
- **verdict.md (this file):** Accuracy — the headline claims only what
  the judged cells carry; council verdicts quoted with their exact
  failing/holding premises; the six carried cadence numbers below are
  byte-copied from the v31 brief, never re-derived; the pre-fill trap hit
  LIVE again this session (a derived discover count briefly written into
  B2 before the log existed) and was caught and reverted to a PENDING
  marker before any commit - the committed cells carry only measured
  values (grep-guarded zero markers before this commit); PASS. Presentation —
  truncated pyramid (answer → bars → findings → consequence →
  reconciliation → carry), typed findings, machine tables; PASS.

## Non-claims

No adjudication of any lettered register item — these artifacts are
instruments; MEASURED-capable is not measured; nothing here reads,
scores, or grades the rendered motion. The at-speed K-S read and the
protocol-vs-viewport question stay owner-sequenced (the artifacts serve
them without claiming them); item (h)'s display-chain half and the
live-camera-true viewport stay named uncovered — the declared sub-window
is the recomposition's own, never the game camera. No multi-creature
scene composition (draw order, neighbors = a separate owner decision).
No register row moves; C1–C5 MET, C6 OPEN doubly gated, untouched;
`docs/integration-readiness.md`, `docs/selection-register.md`,
`docs/state-track-schema.md` (draft-1 HISTORY), and
`docs/replay-capture-design.md` byte-frozen; banked verdict trees
untouched. No schedule claims. Zero new art pixels (composition
re-places banked export bytes only); zero exports; zero release
manifests; zero new tool files (census 4); no capture tooling; no
integration design content; mechanical ids throughout. No cadence
analysis beyond the carry block (observed points only, never a trend).

## Mail and pin status (v31)

- Inbox at step 0: EMPTY (expected — v30 closed every loop). No mail
  arrived mid-session as of this commit. Nothing owed upstream; no mails
  sent (no content hops to note).
- Pins: TWO identity-only hops absorbed. Step 0: `ea0e37c7` →
  `a2f66446` (all five pinned blobs byte-identical, 20/20 constants +
  attack_timing green, mechanical re-pin `df5f4c5`); asset_gate exit 0
  after the re-pin (identity-drift WARNING class only). Zero hops through
  commits A–C (pin_drift re-run before B and C: "no re-pin due"); a
  second hop `a2f66446` → `fb42e6c4` surfaced at the pre-D checkpoint
  (again all blobs byte-identical, 20/20 + attack_timing green, solo
  re-pin `877794b`; the palette anchoring is CONTENT-bound and
  unaffected). asset_gate exit 0 at the gate lattice.

## Cadence carry (BANKED here per the v31 brief's duty; the SIX v30
numbers verbatim — the brief is their only carrier until this bank)

- v30 re-pin `7e66fbc` hook real 2m50.042s = 170.0s green (28th observed; identity-only solo re-pin at session open; box quiet, no concurrent discover, game seat live upstream)
- v30 commit A `474c7d1` hook real 2m53.691s = 173.7s green (29th; 10-file evidence+review commit incl. two multi-MB `-text` track blobs; box quiet)
- v30 commit B attempt 1 hook real 3m4.370s = 184.4s RED (fast-gate red, NOT drift: v14/v15/v16 manifests pin the consumer's sha256 - the pre-registered class, wider than registered; resolved by their own tools' re-banks inside the same change set; labeled EVENT, never a green point)
- v30 commit B `7b0a2dc` hook real 2m58.784s = 178.8s green (30th; code + 55-test module + four manifest re-banks; box quiet; FIRST hook with test_track_v1 in the fast tier; two `WARN [complexity]`/`WARN [crap]` lines = warn-metrics, not gates)
- v30 commit C `9f9bc45` hook real 2m57.899s = 177.9s green (31st; 5 review files; box quiet, post-discover)
- v30 push: total-hook real 176m48.941s = 10608.9s at 748 tests ("v30 close"; window = exactly 4 commits; upstream QUIET - zero hops after step 0; first-attempt green, zero mid-hook drift; 441 advisory WARN vs v29's 434; headroom 50.9% vs 21600s; FIRST POINT AT 748 TESTS - the 693 band CLOSED at ten points spanning 10329.5-12873.5)

Reading (observed points only, never a trend): hook green band 160-268.3s
(n=15, frozen) + sixteen post-bank carry points 244.0 / 166.3 / 159.8 /
180.8 / 188.1 / 261.5 / 171.7 / 172.0 / 173.8 / 190.5 / 249.7 / 257.0 /
170.0 / 173.7 / 178.8 / 177.9; total-hook at 748 has ONE point (10608.9)
and the 748 band CLOSES at it. Post-adoption clean windows n=10,
newest-first sizes 4/6/2/1/1/1/4/1/1/4.

**COMPOSITION BREAK (named per the brief's duty):** v31 adds
`tests/test_runtime_extract.py` (36 tests, fast tier) and 4 SLOW guard
tests — the SECOND suite-composition change since v21 — so v31's push
lands at N=788 tests (discover-measured from the captured log: `Ran 788
tests in 4095.694s` / `OK` / EXIT:0; 748 + 40 exactly accounted) and
is labeled "first point at 788 tests", never pooled with the closed 748
point. The new fast module carries ONE real-composition pipeline unit
(~19s), so v31 hooks sit ~196s — a named shape shift inside the frozen
band, not noise.

v31's OWN reals (each condition-labeled; commit D + push reals land
after this file and ride the session receipt + the v32 brief per the
standing structural-lag mechanism):

- v31 re-pin `df5f4c5` hook real 3m16.203s = 196.2s green (32nd observed; identity-only solo re-pin at session open; box quiet, no concurrent discover)
- v31 commit A `1026dcc` hook real 4m11.641s = 251.6s green (33rd; 1 review file; box quiet; interior of the frozen band, no concurrent load observed)
- v31 commit B `caa31f2` hook real 3m16.333s = 196.3s green (34th; consumer extension + 36-test fast module + 59-line SLOW guards + palette manifest + four manifest re-banks; box quiet; FIRST hook with test_runtime_extract in the fast tier (~+19s pipeline unit); two `WARN [complexity]`/`WARN [crap]` lines = warn-metrics, not gates)
- v31 commit C `5f69824` hook real 3m15.904s = 195.9s green (35th; 4 artifacts + manifest + .gitattributes apng rule; box quiet)
- v31 re-pin `877794b` hook real 4m24.970s = 265.0s green (36th; identity-only solo re-pin, post-discover same session - the post-discover-high pattern v29 named; box otherwise quiet; inside the frozen band max 268.3s)
