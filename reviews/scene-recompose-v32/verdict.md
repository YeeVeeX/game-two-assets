# v32 — first SCENE recomposition: engine-true neighbors around the sprite proposal — verdict (judged)

**Answer first: PASS on all six bars — the banked span now renders as the
engine's real SCENE, and it adjudicates NOTHING. The possessed striker's
span [420, 483] of track 1 (re-derived live, asserted EQUAL to the banked
v31 span; the subject's 64/64 (pose, facing, offset) decisions asserted
EQUAL to the banked v31 capture) composes over the pinned district map's
tile layer (62 floor + 48 wall tiles inside the window; the walls the v31
artifact flattened now draw), with FOUR of the seventeen neighbors
entering the M=4 window as the ENGINE'S OWN primitive bodies (242 draw
events, 846 omission events, all counted): kit-colored 28px rects with
the engine's total facing-notch law — up, left, and diagonal facings DRAW
(the unrenderable-facing refusal class that blocks sprite neighbors
dissolves for primitives by construction). The possession ring draws from
pinned constants at the subject's drawn position, lunge-riding per
renderer bytes. Window [96, 256] 352x320, sheet 2874x2718 (< 4096
asserted), native + 4x APNGs at exact 1/60 s, double-build deterministic,
committed blobs sha-exact, SLOW regeneration guard live and green. Every
layer's provenance is disclosed in pixels; every artifact answers ZERO
register items and says so in pixels, filename, and manifest. C6 stays
OPEN.**

**Authorization (owner order, recorded verbatim per the v32 brief):**
"AUTHORIZATION: the v31 verdict pinned that 'full-scene multi-creature
composition (draw order, neighbors) is a SEPARATE owner decision' - THIS
brief is that decision, owner-launched (record this line in the verdict
as the owner order)."

Review objects: the intaken bundle `20260826T175326Z_p1_42` (track 1
sha256 `dd68c8cb…44545d`, verified live at generation by
`verify_runtime_intake`); the pre-registered protocols (`rationale.md`,
commit A `4b193ae`, cells NULL, every pixel string glyph-checked BEFORE
registration); the two anchored manifests + consumer extension (commit B
`2a5930a`); the artifact set (commit C `8cf047a`,
`scene-exp-manifest.json` repo_commit_at_generation `2f138c5…`). Session
model `us.anthropic.claude-fable-5` (verified from `PI_MODEL`). Council:
cross-vendor adversarial, brief 7,805 B inlining the layer law, value
citations, disclosure strings, and measured stats; Kimi K2.5 FIRST pick
returned FULL labeled verdicts (fallback never fired): **Q1 CONFIRMED,
Q2 CONFIRMED, Q3 CONFIRMED, Q4 UNCERTAIN, Q5 CONFIRMED**; totals
1,983 + 2,946 = 4,929 tokens, within the 8k pooled budget;
reconciliation below.

## The bars, judged

| # | Bar | Verdict |
|---|---|---|
| B1 | Protocol fidelity | PASS — subject law + kit gate + span re-derivation ran forward-only; the span equality assert ran GREEN against the banked v31 `span_frames` [420, 483] at generation (manifest `span_equality.equal` true) and the subject decision stream asserted EQUAL to the banked capture (64/64, manifest `subject_decisions_equality.entries_compared` 64); window = the registered M=4 formula output [96, 256] 352x320, whole-TILE aligned (guard-asserted); draw order = derived faction passes + the DECLARED within-faction tiebreak exactly as registered; sheet wrapped at the registered 8 cells/row; ZERO post-hoc adjustments; no STOP condition bound |
| B2 | Regression | PASS — `--check` exit 0 after every edit batch (672 v10/v11 cells + 340 v9 records green throughout; final state shows BOTH artifact guards "(banked)"); `tests/test_track_recompose.py` diff +68/-0 (pure insertion, the 33 draft-1 tests byte-identical); `tests/test_track_v1.py` + `tests/test_runtime_extract.py` UNTOUCHED; the FIVE-manifest fan-out surfaced exactly as pre-registered and resolved by each manifest's OWN tool — per-manifest git diff = the consumer pin line (`19c63562…` → `8b1c79f9…`) + `repo_commit_at_generation`, and the v31 SHARP condition held: `git diff --name-only` on the v31 tree shows ONLY the manifest, every banked artifact byte-identical; full discover green at N=837 = 788 + 45 (`test_scene_compose`) + 4 (`SceneArtifactGuards`), exactly the new tests (`Ran 837 tests in 4249.691s` / `OK` / EXIT:0, captured `g2a_v32_discover.log`, detached run ~71 min wall, foreign worldsmith seat live on the box — confound named) |
| B3 | Anchoring | PASS — every neighbor value cited to pinned renderer bytes at file+line in `manifests/scene-reference.json` (KIT_BODY :16-21 incl. the `Hash.new(HUMAN_BODY)` default for unlisted kits as a CITED VALUE; notch :43 + :863-877; lunge :879-888; ring :23 + :798-802; order :96-99/:108-109/:843; SIZE :76 + creature.rb:9); tile glyphs cited (:432 + :259 wall, :279+ floor, :349 transition gold) with tiles/transitions copied VERBATIM from the pinned district.json; both new manifests CONTENT-bound (`source_sha256_lf` = the runtime-baseline pins, test-asserted) so identity re-pins never invalidate them; the double-offset trap named and avoided (px already carries the +2 inset — body rects at round_half_up(px)+lunge with NO added tile_offset); generation cross-checks scene-reference == render-reference values live (`scene-reference-mismatch` refusal); the ONLY non-derived element is the within-faction tiebreak, DECLARED and disclosed, its consequence measured (f4); council Q3 CONFIRMED value-by-value |
| B4 | Determinism | PASS — in-process double build byte-identical at generation (manifest flag true; CLI hard-fails otherwise); all four committed blobs re-hashed EXACT against the manifest pins from `git show HEAD:` (post-`-text`-routing proof; `/reviews/**/*.png` + `*.apng` routed `-text` since v31); the SLOW guard (`SceneArtifactGuards`, 4/4) regenerated everything byte-identically from committed evidence (169.2 s, green); `--check` gained the scene guard additively (pre-bank skip pattern held: "(pre-bank skip)" before commit C, "(banked)" after) |
| B5 | EXP boundary | PASS — the SCENE-EXP disclosure set appears in pixels (4-line banner incl. the f4-adopted temporal-selection line `SPAN 420 TO 483 OF WINDOW 420 TO 560  SELECTED SPAN  SEE DECISIONS CAPTURE` + footer `ANSWERS ZERO REGISTER ITEMS`; 2-line APNG headers), in every artifact filename (`scene-exp-`), and verbatim in the manifest + participants capture; every pixel string glyph-checked against the live SEAM_FONT BEFORE commit A (zero live deviations this sprint — the v31 f2 class stayed empty; "RECT BODIES" carries the primitive-body meaning under the no-V font); adjudication-language machine-scan over all artifact strings CLEAN before council; council Q1 CONFIRMED; zero SYNTHETIC labels; zero register/status edits; frozen docs and surfaces byte-untouched; census 4 |
| B6 | HFO gate | PASS — accuracy and presentation judged separately per artifact (section below) |

## Findings (typed per the pre-registered classes)

- **f1 (protocol execution, DONE):** the whole registered pipeline ran
  forward-only behind the LIVE intake gate: subject law → kit gate →
  palette/map/scene-reference loads + consistency cross-check → span
  re-derivation + equality assert → M=4 window → participants capture →
  dims assert → decision-equality assert → double-build composition.
  FOUR neighbors enter the window across the span (`blocker` and `lobber`
  drawn 64/64 — the subject's pack fights beside it the whole span —
  `rusher0` 64/64, `rusher1` 50/64 with 14 omissions counted); 13
  neighbors never intersect and are counted omitted; up/left/diagonal
  facings drew via the engine's total notch law — the track's dominant
  refusal class (v30 n1: `unrenderable-facing` 504) simply does not
  apply to primitive neighbors.
- **f2 (noted, scene-truth fact; council Q2 kernel adopted as
  documentation):** the possession ring renders as a solid white PLATE
  behind the subject because the ENGINE's ring grammar is a solid rect
  drawn UNDER the body (renderer.rb:801-802) and the banked striker
  sprite does not fill its 28px body box — engine-true draw order
  composed with the proposal's body, surfaced by the instrument exactly
  as built. The ring's provenance is disclosed ("possession ring from
  pinned constants"); the artifacts are not edited post-registration. A
  pixel-line naming the plate (e.g. `RING DRAWS UNDER THE BODY`) is
  recorded as a candidate for the NEXT scene generation, owner-sequenced
  (the v31 f4 precedent). The fact itself is register-relevant someday:
  the engine's possession grammar assumes a body that covers the ring's
  center — a non-filling sprite breaks that assumption visibly.
- **f3 (documentation correction, council Q4 UNCERTAIN re-verified and
  adopted):** my council-brief claim C5 ("the not-modeled list covers
  everything") was WRONG as stated. The manifest's not-modeled lists are
  class-true (every named item is truly not modeled; no list claims
  exhaustiveness) but the world-layer enumeration was incomplete. The
  exhaustive census from the pinned `draw()` order (renderer.rb:96-131,
  translate block) banks HERE: draw_map (modeled: floor/wall/transition;
  disclosed-not-modeled: decor, motif, ambient, seal slabs, threshold
  frames, grid-over-walls) → impacts → corpses → stations → drops →
  corpse-load pips → expiry flashes → seal marks → respawn tells →
  humans → pack → enemy strikes → projectiles → taunt cast pulses →
  kill pops → level pops → chant rings → god mark → station
  ledger/cue text — plus the camera-shake translate itself and the
  post-translate screen-space stack (writ veil, HUD, safe chip, controls
  overlay, edge pips, banner, breach line, wipe overlay, ledger beat,
  stagger veil, hurt vignette). Items NOT named in the committed
  manifest lists: volley impacts, corpse-load pips, expiry flashes,
  respawn tells, taunt cast pulses, kill/level pops, station ledger/cue
  text, threshold frames, camera shake, and the screen-space stack.
  EVERY one depends on world state a creatures+masks track does not
  carry — absent by construction, never drawable from this evidence —
  and the class disclosures ("feedback states beyond the ring", "not
  the game camera") remain true. The full enumeration is a candidate
  for the next scene-manifest generation; no committed artifact string
  is false.
- **f4 (measured, council Q5 CONFIRMED adopted):** the DECLARED
  within-faction order (ascending drawn y, tie → name) invents occlusion
  relationships the engine never pinned — kimi's un-named risk was that
  this invented order fakes cross-frame occlusion STABILITY. Measured
  from the evidence this session: exactly ONE same-faction overlapping
  pair exists in the span (`blocker`/`lobber`; the 56 capture events are
  all theirs), it persists across 54 adjacent-frame steps, and the
  declared order flips EXACTLY ONCE — one occlusion-swap event in 64
  frames. The consequence is real, tiny, and now quantified; a
  cross-frame order-stability stat in the participants capture is the
  recorded next-generation candidate. The disclosure ("the proposal's
  own tiebreak") stands.
- **f5 (new pin site, pre-registered consequence):** the v32 scene
  manifest pins the consumer's sha256, so a future `track_recompose.py`
  edit fans out to SIX manifests (v13/v14/v15/v16/v31/v32), each
  regenerated by its own tool under the same lawfulness condition. v33
  inherits this.

## Pre-registered mechanical consequence, resolved as registered

Editing the consumer moved its sha256; all five banked manifests
regenerated by their own tools (`--make-demo`, `--make-audit`,
`--make-artifacts` x2, `--make-runtime-artifacts`); per-manifest diff =
exactly

```
-    "tools/track_recompose.py": "19c63562a3b8...464009be"
+    "tools/track_recompose.py": "8b1c79f902cc...4099bedb"
-  "repo_commit_at_generation": "<prior>",
+  "repo_commit_at_generation": "4b193aea6271...bf7d7aecc",
```

with every banked artifact byte-identical — the v31 SHARP condition
(any v31 artifact byte moving = broken single-creature behavior) held:
only `runtime-exp-manifest.json` changed in the v31 tree. Fan-out beyond
the five = none. The four expected `test_track_recompose` fan-out
failures appeared between the parser edit and the regen and resolved in
the same change set (the v30-commit-B-attempt-1 class; never hit a
hook).

## Structured critique and cross-vendor review

Kimi K2.5 (first pick) returned full labeled verdicts; the pre-registered
fallback never fired. **Q1 CONFIRMED** (no adjudication smell; "'REAL'
describes source, not quality"); **Q2 CONFIRMED** (no under-disclosed
layer; the white-plate visual "engine-true behavior… accurately
described", with the note that f2's typed documentation carries it);
**Q3 CONFIRMED** (anchoring "all values anchor to cited code or declared
invention; no mis-derived values detected", the Hash.new default and the
total notch law explicitly verified); **Q4 UNCERTAIN** (cannot verify a
universal "covers everything" claim from cited lines alone — correct;
re-verified against the full pinned renderer this session and banked as
f3, which CORRECTS my claim); **Q5 CONFIRMED** (the cross-frame
occlusion-stability risk — re-verified by direct measurement, banked as
f4 with the numbers). Per the v12–v31 precedent every adverse or
uncertain claim was re-verified against primary bytes before folding;
all adoptions made a claim smaller or more explicit; artifacts were not
edited post-registration; all adoptions folded before this commit.

## HFO gate (accuracy vs presentation, separately)

- **Disclosure strings (pixels + manifest):** Accuracy — every clause
  states a machine-verifiable fact (captured state from a verified
  bundle; frame selection + composition = the two version-pinned
  proposal ids; per-layer provenance; the selected-span line names its
  own selection and points at the unbiased capture; zero register
  answers), and the one qualitative word ("real") is immediately
  qualified (council Q1 concurs); PASS. Presentation — the 4-line banner
  leads with the class, walks the layers in provenance order, ends the
  eye at the footer's zero-answers law; the manifest carries the full
  prose verbatim plus per-layer citation blocks; PASS.
- **scene-exp-manifest.json + participants capture (machine+owner):**
  Accuracy — every hash recomputed over named bytes (committed blobs
  re-hashed EXACT from `git show HEAD:`); span/window/decision values
  are generation-asserted and guard-reproduced; participants numbers
  re-derived independently this session (the f4 measurement reproduces
  the capture's 56); PASS. Presentation — flat sorted-key JSON, formulas
  restated with their registration pointer, per-layer citations
  compact, no unlabeled precision; PASS.
- **verdict.md (this file):** Accuracy — the headline claims only what
  the judged cells carry; council verdicts quoted with their holding
  premises; the FOUR carried v31 cadence numbers below are byte-copied
  from the v32 brief, never re-derived; discover/push cells were held as
  placeholder markers until their captured logs existed (grep-guarded
  zero before this commit) — and the pre-fill trap hit LIVE again this
  session: a derived N and an INVENTED discover duration were briefly
  written into B2 before the log existed, caught on re-read, reverted to
  placeholder markers, and filled only from the captured log — which
  proved the derived guess WRONG (guess 833, captured 837: the 4 SLOW
  guards were missing from the guess); disclosed here, the v31
  precedent; f3 corrects my own council claim openly;
  PASS. Presentation — truncated pyramid (answer → bars → findings →
  consequence → reconciliation → carry), typed findings, machine tables;
  PASS.

## Non-claims

No adjudication of any lettered register item — these artifacts are
instruments; MEASURED-capable is not measured; nothing here reads,
scores, or grades the rendered scene. The fusion-class items the scene
composition someday serves stay owner-sequenced. Item (h)'s
display-chain half and the live-camera-true viewport stay named
uncovered — the declared sub-window is the recomposition's own (s84
item 2's allowance), never the game camera, and the scene models no
camera behavior (f3 names the shake transform explicitly). No register
row moves; C1–C5 MET, C6 OPEN doubly gated, untouched;
`docs/integration-readiness.md`, `docs/selection-register.md`,
`docs/state-track-schema.md` (draft-1 HISTORY),
`docs/replay-capture-design.md`, and all banked verdict trees (incl.
runtime-recompose-v31) byte-frozen. No schedule claims. Zero new art
pixels (subject = banked export bytes; neighbors/tiles/ring =
engine-anchored procedural primitives of the banked convention class);
zero exports; zero release manifests; zero new tool files (census 4);
no capture tooling; no integration design content; mechanical ids
throughout. No cadence analysis beyond the carry block (observed points
only, never a trend).

## Mail and pin status (v32)

- Inbox at step 0: EMPTY (expected). No mail arrived mid-session as of
  this commit. Nothing owed upstream; no mails sent (no content hops).
- Pins: FOUR identity-only hops absorbed, all mechanical
  (`985a2e30` → `ad7f6a1e` at step 0, re-pin `e42fd5a`; → `775a6c11`
  pre-commit-B, re-pin `3748aa5`; → `6124076a` pre-commit-C, re-pin
  `2f138c5`; → `54b634cf` pre-commit-D, re-pin `defbadc`;
  all five pinned blobs byte-identical each time, 20/20
  constants + attack_timing green). asset_gate exit 0 at step 0
  (identity-drift WARNING class) and at the gate lattice. The two new
  anchored manifests bind to CONTENT (`source_sha256_lf`), so identity
  re-pins never invalidate them (their anchoring `game_commit`
  `ad7f6a1e` records the derivation commit, the v31 palette precedent).

## Cadence carry (BANKED here per the v32 brief's duty; the FOUR v31
numbers verbatim — the brief was their only carrier until this bank)

- v31 commit D `d80e64d` hook real 3m52.837s = 232.8s green (37th observed; 1 review file; post-discover session, box quiet)
- v31 re-pin `4f90462` hook real 3m13.221s = 193.2s green (38th; identity-only solo re-pin pre-push; box quiet)
- v31 push: total-hook real 211m9.406s = 12669.4s at 788 tests ("first point at 788 tests"; window = exactly 7 commits - rev-list verified; THREE identity-only hops absorbed in-session; first-attempt green, zero mid-hook drift; 454 advisory WARN vs v30's 441; headroom 41.3% vs 21600s; foreign imagesmith seat process live on the box mid-window - confound NAMED, not measured)
- v31 discover real 4095.694s at 788 tests (detached, ~68 min wall; first point at 788; the 748 point 3067.9s CLOSED)

Reading (observed points only, never a trend): hook green band
160-268.3s (n=15, frozen) + twenty-three banked carry points (the
twenty-one through v31's five, plus the two above). Total-hook bands:
693 CLOSED at ten points (10329.5-12873.5); 748 CLOSED at one
(10608.9); 788 CLOSES at its single point (12669.4). Post-adoption
clean windows n=12, newest-first sizes 2/7/4/6/2/1/1/1/4/1/1/4 (the
v31 close window = 2 commits: `d80e64d` + `4f90462`).

**COMPOSITION BREAK (named per the brief's duty):** v32 adds
`tests/test_scene_compose.py` (45 tests, fast tier) and 4 SLOW guard
tests (`SceneArtifactGuards`) — the THIRD suite-composition change since
v21 — so v32's push lands at N=837 tests (discover-measured from the
captured log: `Ran 837 tests in 4249.691s` / `OK` / EXIT:0; 788 + 45 + 4
exactly accounted; detached run with a foreign worldsmith seat live on
the box, confound named) and is labeled "first point at 837 tests",
never pooled with the closed 788 point. The pre-fill trap's live catch
this session is the sharpest proof yet: the derived guess briefly
written into B2 said 833 — the captured log says 837 (the 4 SLOW guards
were missing from the guess). The new fast module carries THREE real-composition
pipeline units (~40 s together), so v32 hooks sit ~219-311s — a named
shape shift OUTSIDE the frozen 160-268.3 band's ceiling on one point
(311.0), carried with condition labels, not pooled into the frozen
band.

v32's OWN reals (each condition-labeled; commit D + push reals land
after this file and ride the session receipt + the v33 brief per the
standing structural-lag mechanism):

- v32 re-pin `e42fd5a` hook real 3m39.062s = 219.1s green (39th observed; identity-only solo re-pin at session open; box quiet; pre-scene-module composition)
- v32 commit A `4b193ae` hook real 4m13.377s = 253.4s green (40th; 1 review file; box quiet; pre-scene-module composition, interior of the frozen band)
- v32 re-pin `3748aa5` hook real 5m10.989s = 311.0s green (41st; identity-only solo re-pin; FIRST hook with test_scene_compose in the fast tier — the third composition change's shape shift, ABOVE the frozen band's 268.3 ceiling; box quiet)
- v32 commit B `2a5930a` hook real 3m39.466s = 219.5s green (42nd; consumer extension + 45-test fast module + 68-line SLOW guards + 2 anchored manifests + five manifest re-banks; box quiet; scene-module composition)
- v32 re-pin `2f138c5` hook real 3m49.748s = 229.7s green (43rd; identity-only solo re-pin; box quiet; scene-module composition)
- v32 commit C `8cf047a` hook real 3m42.765s = 222.8s green (44th; 5 artifact files incl. the 1.38 MB zoom4 APNG; box quiet; scene-module composition)
- v32 discover real 4249.691s at 837 tests (detached, 70m49.984s wall; FIRST POINT AT 837 TESTS — the 788 band CLOSES at its single point 4095.694s; foreign worldsmith seat live on the box, confound named)
