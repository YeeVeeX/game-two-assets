# Calibration v9 — cross-facing onset-seam verdict (facing change mid-tween)

Reviewed artifacts:

- `reviews/calibration-v9/cross-seam-sheet.png` — SHA-256
  `8b41f90df03e9672efa2fa78a42889f5a2d60fa1111a2c483fd5d047b9b99006`,
  regenerated byte-identically (in-process double-build in
  `cross_seam_metrics` plus a full CLI re-run compared with `cmp`).
- `reviews/calibration-v9/cross-seam-metrics.json` — SHA-256
  `791d3b0eec705152f8db10868f4d5790de2b0fc2e6aa473789c59faafdf0a419`,
  regenerated byte-identically under identical flags (`cmp` clean).
- Viewing aids (non-blocking): `cross-lanes-dr.apng` (`609a7ff7…`) and
  `cross-lanes-rd.apng` (`89038c52…`) — the four cross lanes side by
  side, 34 frames, exact 1/60 s per-frame delay via the banked APNG
  encoder, 0.5 s final hold, infinite loop, 4x nearest-neighbor;
  byte-identical on regeneration and equal to committed bytes
  (machine-checked; the degenerate lanes are v8's CONTROL, animated in
  the banked v8 aids).
- **No release, no new pixels:** this sprint composed banked bytes only.
  All 26 banked export pins across the seven releases (v0–v3, v5–v7)
  byte-verified; `exports/` contains exactly the pinned files and no
  `calibration-v9` directory; the `make_release.py` registry and
  `tools/export_assets.py` are untouched (machine-enforced).
- Baseline: two mechanical identity re-pins this sprint. Step 0:
  `74fb3b8b…` (v18 session-10/11 docs activity, drift docs-only; all five
  pinned hashes LF-identical, `attack_timing` re-verified 5/4/8/13 at
  the new commit via `/kits/striker/...`; the sprint-9 reachability
  facts re-verified at the same commit: `face` L142–144 unconditional,
  `start_attack` L153–157 carries no `moving?`/facing guard,
  `controllers.rb` L52→L56→L64 order, `front1` L185–186 reads live
  `@facing`, `grid_walker` tween facing-blind with no snap — the
  cross-facing seam class is REACHABLE at the pin). Banking checkpoint:
  `85419806…` (sibling audio-lane/docs work; `attack_timing`
  re-verified 5/4/8/13; `controllers.rb`/`creature.rb`/`grid_walker.rb`/
  `world.rb` blob-identical across the hop — no guard appeared, the
  class stays reachable). Both committed alone. Live gate exit 0 at
  step 0 and immediately before banking.
- Vision-critical judgment: session model `us.anthropic.claude-fable-5`
  (verified from `PI_MODEL`, not self-report), on the committed sheet
  plus deterministic band crops of the same bytes (ephemeral
  diagnostics; native 1x decides, both zones, both pairs, plus the 2X
  strips and 3x onset zooms for diagnosis). Council seat cross-vendor
  (Kimi K2.5), one consolidated adversarial verdict, 3,320 tokens of
  the 8k cap (1,723 in / 1,597 out, single call, no truncation),
  response redirected to a file and read as UTF-8.

Question under test (the banked v8 next-hypothesis): when the attack
input carries a facing change mid-walk — legal and stable at the pin —
does the fN(A)→w0(B) cut read as one action beginning (turn-and-coil),
not an identity break; do the attack poses keep their banked reads while
the lunge offset runs PERPENDICULAR to the residual tween; does the
strike keep salience; and which tile does the perpendicular strike
visually bind to? Zero new frames; 8 per-class-per-pair sub-verdicts +
the degenerate regression gate; FAIL routes to an ENGINE-TERRITORY
finding extending the v8 recommendation, never a rescue frame.

## Accuracy — all-must-pass (the pre-registered INTEGRITY bars)

| Item | Verdict | Evidence |
|---|---|---|
| Zero new exports; 26/26 banked pins byte-verified; no v8/v9 exports dirs; exporter/registry untouched | PASS | `cross_seam_metrics --check` export bar |
| 2D seam jump tables complete: onset−2..onset+10 (EARLY +12), pose Δ% + (dA,dB) + window (dx,dy) + exact integer squared magnitude per consecutive pair | PASS | committed metrics, 2 pairs × 5 lanes (14 rows EARLY, 12 others) |
| **Degenerate regression (hard):** DEGEN-D/DEGEN-R rows machine-equal to the committed v8 CONTROL rows — pose names, pose Δ%, dA == the v8 scalar, dB == 0, row for row | PASS | `degenerate_regression_failures: []` against `reviews/calibration-v8/seam-metrics.json`; also pure-plan equality against the v8 plan (test-enforced) |
| Cross-facing context deltas reported; idle↔idle reproduces the banked 44.44 exactly | PASS | f0 53.64 / f1 45.00 / f2 36.45 / f3 44.44 / idle 44.44 (f3 = idle byte-copy — the equality is a machine self-check of the byte-level derivation) |
| Tick math exact: w0×1+a0×4 at −3∥B, k0×4 at +6∥B, s0×1+r0×6+x0×1 at 0; walk frames facing A pre-onset; attack facings B from onset; tween deltas 1,1,2,3,4,3,4,3,4,3,2,1,1; REM 11/8/4/0/0/0; arrival phases rec 3 / act 4 / wind 5 / pre-onset ×3; r0 hold 6 ticks | PASS | `--check`, 0 failures; no tick added, consumed, or reallocated |
| In-window by construction: every draw vector + the [2,2,29,29] contract bound inside the window dims (lane windows AND onset crops), proven from the plan, never from put() clipping; both candidate strike tiles + grid lines inside every lane window | PASS | `bounds_failures: []`; cross windows 2 A-tiles × 3 B-tiles [back\|creature\|arc]; degenerate windows = the v8 3-tile axis window |
| Compositor byte-determinism (sheet, metrics, both APNGs) | PASS | double-build SHAs above; CLI re-run `cmp` clean; committed bytes equal fresh builds |
| Composition purity: every creature cell = banked export bytes at the computed 2D draw vector | PASS | 414 cells, dual verification (region reconstruction + direct export-byte equality), 0 failures |
| Banked sheets v0–v8 regenerate byte-identical under the extended toolchain; coverage ≥ 80 | PASS | full gate 339 tests green; coverage 94% total (new tools 99% / 82%) |
| Pass bars pre-registered before artifacts existed; INTEGRITY/MEASUREMENT split fixed in the rationale before any number | PASS | rationale + toolchain committed (change set 1) before the sheet was generated (change set 2) |
| No game-two changes | PASS | read-only `git show` against `../game-two` only |

**Accuracy: 11/11 integrity bars.** The pre-registered MEASUREMENT bar
(release salience) fired — that is evidence, not accuracy failure; it is
adjudicated in rubric line 3 below, exactly as the rationale fixed
before any number existed.

## Presentation — measured evidence (conventions inline, per the banked v8 council-Q1 law)

Conventions for every table: pose Δ% = 100·XOR/union computed on export
bytes in SPRITE-LOCAL space — the draw position never enters the XOR
(council Q6 duty adopted: tween displacement cannot contaminate pose
deltas; displacement lives only in the (dA,dB) columns). w0 IS windup
tick 1 and draws at the windup offset (−3 ∥ B). dA = along the walk
axis, positive in the walk direction; dB = along the attack facing,
positive toward the arc tile; squared = dA²+dB², exact integer. DR
attacks RIGHT (release pose 31.97); RD attacks DOWN (27.18).

| Onset-cut table (pose Δ% / (dA,dB) / sq) | EARLY | MID | LATE | CONTROL | DEGEN |
|---|---|---|---|---|---|
| DR cut fN@down→w0@right | f0: **48.14** /(1,−3)/10 | f0: **48.14** /(4,−3)/25 | f2: **39.88** /(4,−3)/25 | f3: **43.70** /(0,−3)/9 | f3→w0 same-facing: 16.48 /(−3,0)/9 |
| RD cut fN@right→w0@down | f0: **49.15** /(1,−3)/10 | f0: **49.15** /(4,−3)/25 | f2: **40.12** /(4,−3)/25 | f3: **43.86** /(0,−3)/9 | 19.69 /(−3,0)/9 |
| free-turn context (same frame, down↔right byte pair) | f0↔f0 53.64 | 53.64 | f2↔f2 36.45 | f3↔f3 44.44 | — |

Pairing rule for the context column (pre-registered role: magnitude
calibration only — the pure-turn lane was never rendered): each cut is
set against ITS OWN frame's cross-facing byte delta. EARLY/MID cuts sit
5.50/4.49 pt BELOW their frame's free turn; CONTROL sits 0.74/0.58 pt
below the idle-turn; LATE sits 3.43/3.67 pt ABOVE its frame's (f2's
36.45 is the smallest context in the set). The onset displacement is
DIAGONAL mid-tween — (4,−3) at MID/LATE peak — where v8's along-facing
onset scalars (−2/+1/+1/−3) were the same components collapsed onto one
axis; disclosed so the tables read correctly.

| Release + salience (per lane) | EARLY | MID | LATE | CONTROL | DEGEN |
|---|---|---|---|---|---|
| a0→k0 (dA,dB) / sq | (4,9) / **97** | (3,9) / **90** | (0,9) / **81** | (0,9) / **81** | (0,+9 along axis) / 81 |
| max other squared in lane | 40 (k0→s0 (2,−6)) | 36 | 36 | 36 | 36 |
| squared axis strictly dominant | YES ×8 cross + ×2 degen | | | | |
| release pose Δ% vs largest other (the onset cut) | 31.97/27.18 vs 48.14/49.15 | vs 48.14/49.15 | vs 39.88/40.12 | vs 43.70/43.86 | 27.18/31.97 vs 17.25/21.31 |
| **pose axis strictly dominant** | **NO** | **NO** | **NO** | **NO** | YES |

The pre-registered MEASUREMENT bar fired on the pose axis in ALL 8
cross lanes (machine `--check`: 8 MEASUREMENT FAIL lines, 0 INTEGRITY)
and passed on the squared-displacement axis in all 10.

Strike-anchoring geometry (derived exactly as pre-registered from
banked k0 bboxes — down [8,6,23,27], right [7,7,28,27] — the 12
crossing pixels per pose, and plan positions; the arc-side grid line at
window-B 64; crossing depth is TWEEN-INDEPENDENT cross-facing because
the B-offset never mixes with the tween: constant 3px DR / 2px RD —
equal to the banked v4 anchoring constants, an independent cross-check
of the derivation):

| Crossing px TRUE arc / NEAR (wrong) tile, per active tick | EARLY | MID | LATE | CONTROL |
|---|---|---|---|---|
| DR (depth 3px const) | **4/8, 11/1, 12/0, 12/0** | 12/0 ×4 | 12/0 ×4 | 12/0 ×4 |
| RD (depth 2px const) | **6/6, 10/2, 12/0, 12/0** | 12/0 ×4 | 12/0 ×4 | 12/0 ×4 |
| body A-overlap TRUE/NEAR px (first two active ticks) | DR 14/7→17/4; RD 10/6→13/3 | full/0 | full/0 | full/0 |

v8 along-facing comparison at the same REM (the committed v8 table):
EARLY never crossed (0/4 ticks, 2–13px short); MID crossed only on its
final tick(s), 0–3px; LATE/CONTROL full. Cross-facing, the crossing
EXISTS at full constant depth on every active tick in every lane — the
failure axis moved from WHETHER the strike reaches a tile to WHICH tile
it enters first.

## Presentation — rubric line by line (pre-registered; judged at native 1x on the committed sheet, both zones, both pairs; every claim sheet-scope — never temporal fusion at 60 tps)

1. **The fN@A→w0@B cut reads as ONE action beginning (turn-and-coil).**
   PASS at sheet scope, all 8 cross lanes, both zones. At 1x, 2X, and
   the 3x onset zooms, the cut reads as the creature TURNING into the
   coil: the down-facing walker's front-on two-eye head becomes the
   right-profile snout (DR) / the right profile turns to camera (RD)
   while body mass, the frozen 5-color ramp, the eye accent, and the
   feet row carry continuously — one creature rotating, never a second
   creature or a swap error. The magnitude evidence agrees at its
   registered scope: the cuts (39.88–49.15) sit inside the free-turn
   context band (36.45–53.64) — EARLY/MID BELOW their frame's own
   facing-swap delta (the coil component is visually absorbed into the
   turn) — and the degenerate baseline (16.48/19.69) shows what the cut
   costs without the turn. Honest disclosures, held to at 1x: (a) the
   EARLY/MID cuts EXCEED the 44.44 cross-facing identity reference
   (48.14/49.15) — pre-registered as legitimate (a facing swap IS most
   of the pixels; no ceiling bar applies), and the identity read
   survives on structure, not magnitude; (b) whether a ~48% one-tick
   compound (turn+state) fuses or double-strobes at 16.7 ms is temporal
   — routed to the runtime item as (j); (c) the context yardstick is a
   byte-pair calibration, NOT a rendered pure-turn lane (council Q2
   adopted) — L1's PASS rests on the native read of the rendered cuts,
   which contain the turn.
2. **The attack poses riding the perpendicular tween keep their banked
   reads.** PASS at sheet scope, all 8, both zones. The held a0
   repeated identically while gliding 1–4px per tick PERPENDICULAR to
   its facing reads as the coiled body carried by momentum — no leg
   alternation, nothing walk-like, and the drift direction (the walk
   axis) is visibly the direction the creature was already moving, so
   it does not read as knockback or ice-slide at 1x; w0's sink, k0's
   gape-and-brace, s0/r0's settle keep their banked identities (the
   recovery-overlap ticks judged under the DECLARED drawing model; the
   engine draws no poses; recovery-walk priority stays a carried
   finding). The perpendicular offsets never fight the tween — dB
   carries the grammar (−3/+6/0), dA carries the walk — and the sheet
   shows them as visibly independent motions.
3. **The strike keeps salience.** **FAIL at the pre-registered scope,
   all 8 cross lanes.** The line as fixed requires the release to be
   the sharpest single-tick event WITH the machine table agreeing on
   both axes separately; the machine table refuses on the pose axis in
   every cross lane (onset cut 39.88–49.15 > release 31.97/27.18 —
   8/8), and per the rationale's fixed semantics that red is
   measurement evidence feeding this sub-verdict, not a waivable
   advisory (Rule 6: a pre-registered gate is never downgraded after
   firing). The graded story is recorded verbatim: the NATIVE event
   read favors the release (it remains the only displaced boundary, the
   only grid-line crossing, the largest squared displacement 81–97 vs
   ≤ 40, and the gape is the most violent shape change on the band),
   and the pose-axis inversion is structural, not artistic — the cut
   carries the facing-swap baseline (36.45–53.64 for ANY frame pair
   across facings), so a cross-facing cut out-XORs a same-facing
   release almost by construction (council Q1 kernel, adopted as the
   EXPLANATION and as a bar-design lesson for future cross-facing
   sprints: pose-axis dominance must be pre-registered
   within-facing-category next time). The remedy attribution is
   engine-side (v4 EXP-row class): splitting the turn from the onset
   (buffer/gate) restores the banked salience order without touching a
   pixel.
4. **Anchoring: the perpendicular strike binds a tile.** PASS for
   MID/LATE/CONTROL, both pairs — the strongest anchoring result in
   the series: full-depth crossing (3px/2px, the banked v4 constants)
   into the TRUE arc tile with 12/12 crossing pixels on EVERY active
   tick, including MID (REM 8), whose along-facing twin managed only a
   last-tick 0–3px crossing in v8. **FAIL for EARLY, both pairs
   (mis-anchor, named):** on the first active tick the strike's
   crossing pixels enter the WRONG tile — DR 8/12 in the NEAR tile
   (T0+B̂) at 3px depth vs 4/12 true; RD split 6/6 — and the bind
   migrates to the TRUE tile only by the third tick (11/1 → 12/0; 10/2
   → 12/0). The strike visually strikes first the wrong tile, then the
   right one — temporal correction, not spatial accuracy (council Q3
   wording adopted; CONFIRMED verdict). Against the v8 yardstick the
   destination now EXISTS everywhere (the tween-independent B-offset) —
   but at REM 11 it initially lies about which tile the hit lands in,
   which is worse for gameplay truth than v8's visibly-missing
   destination: a player reads a hit on a tile the engine never
   targets.
5. **Degenerate lanes read exactly as v8's CONTROL; identity holds
   sheet-wide.** PASS. Machine-equal to the committed v8 CONTROL rows
   (pose AND both position components, row for row — plus pure-plan
   equality test-enforced), and the native read of the DEGEN bands is
   the banked t13–t28 grammar in the new window geometry; byte-frozen
   frames at computed 2D vectors (414 cells dual-verified), frozen
   ramp, feet/anchor law inside the moving cells, nearest-neighbor
   everywhere, FILM rows anchoring both facings' identities.

**Presentation scores (accuracy scored separately above):**

- **DR section: 8.5/10.** The 2×3-tile windows make the two candidate
  strike tiles a single glance; EARLY's wrong-tile first crossing is
  visible against the drawn grid junction — the sheet's design caught
  the new 2D failure mode exactly where the overlap table said to look.
- **RD section: 8/10.** The vertical-B windows carry the down-strike
  crossings legibly; the 3-tall cells cost vertical density (the RD
  section is the sheet's tallest), never the read.
- **Sheet as artifact: 8.5/10.** Five stacked bands per pair put the
  four onset classes and the along-facing baseline in one eye
  movement; the onset strips (crop pre-registered) isolate the turn
  cut beside the degenerate non-turn cut — the single most diagnostic
  comparison on the sheet.

## Structured critique and cross-vendor review (Kimi K2.5, adversarial)

One consolidated verdict (single call, 3,320 of 8k tokens); every
numeric claim recomputed against pixels/numbers before adoption
(models see text, not pixels). Council posture: REJECT-leaning on the
routing, CONFIRM on the geometry. Reconciled claim-by-claim:

1. **Q1 "pose-axis bar is a category error; route the sub-verdicts to
   the native read (PASS)" — kernel ADOPTED, re-routing REJECTED.**
   The council's structural analysis is verified correct: facing-swap
   XOR (baseline 36.45–53.64 for ANY cross-facing frame pair) and
   same-facing motion XOR (27.18/31.97 release) are different
   distributions, so the bar was structurally destined to fire for this
   class — a genuine bar-design finding, recorded above and in the
   findings list. But re-routing the sub-verdicts to PASS would
   downgrade a pre-registered gate AFTER it fired — exactly the move
   Rule 6 and this series' pre-registration discipline exist to
   prevent (the rationale fixed the red-bar semantics before any
   number). The sub-verdicts stay FAIL at the registered scope; the
   category-error analysis becomes the finding's explanation and the
   next-sprint bar-design lesson; the engine-side remedy (split turn
   from onset) simultaneously restores the machine bar — the FAIL
   routing and the council's substance point at the same fix.
2. **Q2 free-turn yardstick — UNCERTAIN, precision ADOPTED, post-hoc
   charge REFUTED.** Correct that the context deltas are byte-pair
   measurements, not a rendered turn lane — the rationale pre-registered
   exactly that role ("reported so the onset cut is judged against the
   turn the engine already permits; the pure-turn lane itself is out of
   scope"), so the yardstick's role was fixed before artifacts, not
   selected post-hoc; the pairing rule (each cut vs ITS OWN frame's
   cross-facing delta) is now stated explicitly under the onset-cut
   table, and L1's wording rests on the native read with the context as
   calibration. The council's demand sharpened the ambiguous "+3.4pt"
   into the explicit LATE row (39.88 vs f2↔f2 36.45 = +3.43).
3. **Q3 EARLY mis-anchor FAIL — CONFIRMED by the council** from the
   8/12 / 6/6 splits; its framing ("first the wrong one, then the right
   one — temporal correction, not spatial accuracy") is adopted into
   rubric line 4.
4. **Q4 breakpoint-table loosening — UNCERTAIN (council lacked the v8
   table), verified here:** the committed v8 metrics show EARLY 0/4
   ticks crossing (2–13px short) and MID last-tick-only 0–3px
   along-facing; this sprint's committed metrics show full-depth
   crossing every active tick in every cross lane. The claim stands
   with both committed tables cited; the breakpoint table is
   re-stated per-axis in the findings (crossing-existence vs
   bind-target).
5. **Q5 recommendation risks — ADOPTED (specification duty), one
   arithmetic detail REFUTED.** Adopted: "facing-lock during tween"
   was underspecified — it must mean gating `face()` on `moving?`
   (turns commit at tile boundaries), which makes the cross-facing
   class UNREACHABLE and costs up to ~13 ticks (~217 ms) of turn
   latency worst-case — a heavy feel cost, stated plainly; locking to
   the NEW facing is incoherent (B does not exist until the input).
   Refuted: the one-tick buffer does not "change the 13-tick walk
   cadence" — the walk tween is untouched; only `start_attack` defers
   one tick, and only when the same input tick changed facing
   (~16.7 ms cost on turn-attacks only).
6. **Q6 unthought risk: "canvas-aligned XOR may be tween-contaminated"
   — REFUTED with the code path, disclosure duty ADOPTED.** Pose
   deltas are computed on export bytes in sprite-local space
   (`pair_stats` over `load_opaque(export)` — the draw vector never
   enters the comparison); machine cross-checks: the degenerate rows
   reproduce v8's numbers exactly under the same convention, and
   f3↔f3 equals idle↔idle (44.44) exactly because f3 is the idle
   byte-copy. The conventions block above now states this explicitly —
   the council's inability to verify it from text is the same
   presentation lesson v8's Q1 banked, extended one clause.

No council claim survived re-verification that contradicts the
decision below at its pre-registered scope; one explanation, one
specification duty, two precision duties, and one bar-design lesson
were adopted; the re-routing recommendation was rejected on Rule-6
grounds with its substance folded into the findings.

## Decision — 8 per-class-per-pair sub-verdicts + the degenerate gate (pre-registered rule applied)

The rule (fixed before artifacts): a class+pair PASSES only if rubric
lines 1–4 pass for it and line 5 holds sheet-wide. Line 5 holds; line 3
fails everywhere at its registered scope; EARLY additionally fails
line 4. Therefore:

- **DR/EARLY and RD/EARLY (REM 11): FAIL on two independent grounds —
  banked as ENGINE-TERRITORY findings.** (a) Anchoring: the strike's
  first active tick binds the WRONG tile (DR 8/12 crossing px in
  T0+B̂ at 3px depth; RD 6/6 split), migrating to the TRUE arc tile by
  tick 3 — the committed-tile lag made visible as a lie about the hit
  tile. (b) Salience: the machine pose axis inverts (48.14/49.15 vs
  31.97/27.18). The failing variable is INPUT TIMING (a turn, an
  attack, and REM 11 of tween sharing one tick), never the pose.
- **DR/MID, DR/LATE, DR/CONTROL, RD/MID, RD/LATE, RD/CONTROL: FAIL on
  the salience machine bar ALONE — banked as ENGINE-TERRITORY findings
  with the grading recorded.** Everything else about these six lanes
  reads: the turn-and-coil cut (L1 PASS), the perpendicular glide
  (L2 PASS), and the cleanest anchoring in the series (L4 PASS —
  full-depth TRUE-tile crossing on every active tick, including
  REM 8, which was marginal along-facing). The single failing axis is
  the pre-registered pose-dominance half of L3: the facing-swap cut
  out-XORs the release 8/8 (39.88–49.15 vs 31.97/27.18), a structural
  property of cross-facing cuts (council Q1), unprovable-at-speed on a
  sheet, and conservatively banked as NOT the answer for these classes
  while the compound remains temporally unverified.
- **Degenerate gate: PASS.** DEGEN-D/DEGEN-R machine-equal to the
  committed v8 CONTROL rows (pose and both position components) and
  reading as banked at 1x — the 2D toolchain is regression-proven
  against the banked series.

**Recommendation recorded for the future runtime-replay integration
design (game-two decisions for AFTER the cycle closes; never
implemented from here, never compensated with pixels):**

- **Facing-aware onset buffering (primary):** when `face(dir)` changes
  facing on the same controller tick as `start_attack`, defer the
  attack one tick so the turn renders before the coil — cost 16.7 ms,
  only on turn-attacks; the onset cut becomes along-facing (v8's
  measured classes) and the banked salience order is restored by
  construction. The walk cadence is untouched (council Q5 detail
  refuted).
- **Facing-lock during tween (alternative, specified per council Q5):**
  gate `face()` on `moving?` so turns commit at tile boundaries —
  makes this class unreachable, but costs up to ~13 ticks (~217 ms)
  turn latency worst-case; the feel trade is runtime territory.
- **EARLY additionally inherits the v8 REM-gate recommendation**
  (buffer/gate mid-walk onset while REM > 8), now with the cross-facing
  evidence that the failure at REM 11 is a WRONG-TILE first bind rather
  than a missing crossing.

What this sprint does **not** claim: temporal fusion or strobe behavior
of the ~40–49% one-tick turn+coil compound at 60 tps, attention capture
by the release under motion, the at-speed read of EARLY's migrating
bind, recovery-walk pose priority (drawn per the declared model only),
or any runtime integration — those are runtime replay-capture
questions, blocked by the one-way boundary. The x0 banking-reversal
condition at the head of the consolidated runtime item is untouched; no
tick was consumed; the 6-tick r0-hold floor stands; the lane-B grammar
remains 16 byte-frozen creature frames.

Status per contract: **measurement banked; selectable candidates
unchanged.** No integration — the game cycle is open in game-two; the
one-way boundary and every runtime-integration stop condition in
`docs/asset-contract.md` hold. The cross-facing onset class is now
measured: the pose-only seam classes at asset level are complete
(along-facing v8, cross-facing v9), with every FAIL routed to engine
territory and zero pixels changed across both sprints.

## Findings for any future integration design (the FULL v8 standing list carried unmodified, extended)

- **Moving-recovery pose priority** (carried, v5–v8): walking is legal
  during recovery; walk-vs-r0 priority undecided — x0 adds the
  recovery-tick-8 sub-case; v8 added EARLY's s0/r0-on-moving-base
  rendering. **v9 extension:** the cross-facing EARLY lanes render the
  same overlap with the settle family in facing B over residual
  A-tween (declared-model-conditional, as always).
- **Exhaust-tail invisibility** (carried, v5): ~18 post-recovery locked
  ticks remain unmarkable asset-side.
- **Runtime temporal confirmation (single consolidated item, extended —
  the x0 banking-reversal condition stays at its head):** replay
  capture should verify (a) the diminished ready-again beat reads as
  the unlock cue at 60 tps and r0→x0 does not fuse into the held span
  (v7 reversal condition); (b) x0 at 16.7 ms is not misread as an
  s0-variant; (c) the r0 hold reads as settle at 100.0 ms; (d) the
  carried v6 checks — w0/s0 strobe/fusion, w0 coil-onset binding,
  66.7 ms a0 hold; (e) k0→s0 −6px reads as recoil; (f) the ACC
  flash-accent under live flicker (v4); (g) mid-walk onset cuts fuse
  rather than strobe at 16.7 ms + the MID/LATE retreat-cue inversion
  (v8); (h) enlarged mid-walk release displacements read as attack,
  incl. the sample-hold smear check (v8); (i) MID's along-facing
  last-tick shallow crossing binds at speed (v8). **v9 additions:**
  (j) the cross-facing turn+coil compound (~40–49% one-tick pose
  change, the largest cuts measured in the series) fuses into one
  perceived event rather than double-strobing, and the release keeps
  ATTENTION dominance at speed despite the larger preceding pose
  delta — the sheet-unprovable half of this sprint's L3; (k) EARLY's
  migrating bind (wrong tile → true tile across 3 active ticks) does
  not bind the eye to the wrong tile at speed.
- **Onset-anchoring geometry (v8 headline, RE-STATED PER AXIS with the
  v9 evidence):** along-facing, the failure axis is
  crossing-EXISTENCE — breakpoints REM ≤ 4 clean, REM 8 marginal
  (last-tick 0–3px), REM 11 absent. Cross-facing, the B-offset is
  tween-independent, so the crossing always exists at full constant
  depth (3px/2px = the banked v4 constants) — the failure axis is
  BIND-TARGET: REM ≤ 8 clean (TRUE-tile, every tick), REM 11 first-tick
  WRONG-tile bind (DR 8/12, RD 6/6 split) migrating true by tick 3.
  Any integration design that keeps unguarded mid-walk onset must
  handle both axes; the v4 EXP-row attribution holds — the failing
  variable is timing, never the pose.
- **Cross-facing onset-cut magnitude (NEW, the v9 headline):** a
  same-tick facing-change attack onset produces the largest one-tick
  pose deltas in the banked series (39.88–49.15%), sitting in the
  free-turn band (36.45–53.64) and above the 44.44 identity reference
  at EARLY/MID — structurally guaranteed to out-XOR any same-facing
  release (the facing-swap baseline). At sheet scope the cut reads as
  turn-and-coil (identity carried by structure); the salience order on
  the pixel-change axis inverts everywhere. Remedy engine-side: split
  turn from onset (one-tick facing-render buffer) or lock facing
  during tween (specified above); both restore the banked order
  without touching pixels.
- **Bar-design lesson (NEW, process):** cross-facing sprints must
  pre-register pose-axis dominance bars WITHIN facing-category (or an
  explicitly cross-category design) — a same-facing release can never
  win a raw XOR contest against a facing swap. Fixed for future
  rationales; NOT patched retroactively here (Rule 6).
- **Onset retreat-cue inversion (carried, v8):** mid-walk onsets
  surrender CONTROL's −3px pullback. **v9 extension:** cross-facing,
  the retreat cue survives as the dB = −3 component on EVERY class
  (the offset axis is orthogonal to the tween), but rides a diagonal
  (dA up to +4) — the pullback reads only in the B axis.
- **Unmeasured onset sub-classes (recorded, not measured):** onset on
  the step-initiation tick (REM 13), onset ON the arrival tick (REM 0
  at t14), facing drift DURING windup (B released/rolled to C — reachable
  at the pin), and the PURE-TURN lane (walk-A→walk-B mid-tween, no
  attack — the rendered yardstick council Q2 asked for).
- **r0-hold floor** (carried, v7): 6 held r0 ticks is the FLOOR —
  untouched by this sprint (zero tick changes, machine-asserted).
- **Facing-mirror handedness** (carried, v5–v8): unchanged; zero new
  frames. The cross-facing cuts add evidence that the two facings'
  silhouette families are far apart (36.45–53.64) — a mirrored facing
  set would change every context number; re-verify then.
- **Enemy-side reuse** (carried, v4–v8): divided-attention
  re-verification extends to cross-facing onsets — on an enemy body a
  turn-attack is a re-aim tell; the wrong-tile first bind (EARLY) is a
  mis-telegraph there, likely worse under divided attention.
- **Bridge-lean asymmetry** (carried, v6/v7): unchanged; no new bridges.
- **Translation-registry discipline** (carried, v7/v8): unchanged; no
  new states or bridges — the registry gained no entries this sprint.

## Next hypothesis (single, for a future bounded sprint — not started)

The runtime-replay-capture integration design remains the highest-value
next step (carried v7→v8→v9 — it now holds four sprints of temporal
questions, (a)–(k), and it is gated on the game cycle closing). If a
calibration sprint happens first, the sharpest asset-level gap this
sprint exposed is the **pure-turn lane**: walk-A→walk-B mid-tween with
NO attack — the rendered yardstick every cross-facing judgment leaned
on as byte-pair calibration only (council Q2). It is the smallest
possible lane set (two pairs, the four v8 onset ticks reused as turn
ticks, zero new frames, the v9 toolchain reused as-is), and it would
settle whether a bare mid-tween facing swap reads as a turn at 1x —
the premise under both this sprint's L1 PASSes and the buffering
recommendation's "render the turn first" clause.

## Stop

Sprint 9 stops here: one reviewed cross-facing sheet, one banked
verdict, two non-blocking APNG aids, one metrics JSON, ZERO new
creature frames, zero new exports, zero tick changes, two mechanical
identity re-pin commits plus two bounded change sets. No smear frames,
no rescue edits, no bar softening, no facing-drift measurement, no
pure-turn lane, no recovery-walk adjudication, no 8-direction set, no
terrain, no enemies, no pack, no game-two changes. The runtime-replay
integration design stays parked until the game cycle closes.
