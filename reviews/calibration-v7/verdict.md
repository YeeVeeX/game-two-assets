# Calibration v7 — completion-rise smoothing verdict (ready-again boundary)

Reviewed artifacts:

- `reviews/calibration-v7/timeline-sheet.png` — SHA-256
  `ef1abca1dc4caed481ec0551a1a776a4fd41356d5a84bb656e71393e71cdd1cd`,
  regenerated byte-identically (in-process double-build in
  `rise_metrics --check` plus a full CLI re-run compared with `cmp`).
- `reviews/calibration-v7/timeline-metrics.json` — SHA-256
  `a963a3de792a342461769eddd038c39752ba626f07ca5f8e3581bd7f885c544d`,
  regenerated byte-identically under identical flags.
- Viewing aids (non-blocking): `timeline-ab-down.apng` (`d353809d…`) and
  `timeline-ab-right.apng` (`9e48a6f9…`) — timelines A | B side by side,
  34 frames, exact 1/60 s per-frame delay via the banked APNG encoder,
  0.5 s final hold, infinite loop, 4x nearest-neighbor; byte-identical
  on regeneration and equal to committed bytes (machine-checked).
- Release under review: `exports/calibration-v7/release.json` — 2
  creature exports (`player_1_lane_b_attack_{down,right}_x0`, the
  sprint's only new pixels), full hash chain, provenance origin
  `procedural` with derivation notes naming the in-between's endpoints
  (frozen v5 r0 → frozen v0 idle) and its direction residues,
  gate-valid at the live pin.
- Banked inputs (untouched): v0 idles, v1 walks, v2 strike keys, v3
  coils, v5 settles, v6 bridges — all 22 consumed exports hash to their
  banked release.json pins across the seven releases (frozen-state
  protection). All banked sheets (v0–v6) regenerate byte-identical
  under the extended toolchain (regression suites green — 248 tests).
- Baseline: two re-pins executed this sprint. One **content
  re-baseline** at step 0 (`88fd36d…` — the EXPECTED renderer.rb
  sustain-presentation drift, executed approve-by-default per the
  dev-seat standing protocol: 36 insertions 0 deletions, all inside
  `draw_station_cue`, creature draw path untouched; EVERY
  `render-reference.json` constant value-re-verified unchanged at the
  new commit — lunge offsets −3/+6/0, hurt flash + flicker 3, ALLY_DIM,
  KIT_BODY striker, telegraph swell 8/4/2/5, possession ring expand 3,
  attack_timing 5/4/8/13; verification table in the re-pin commit) and
  one mechanical identity re-pin (`29edda31…` — v18 increments 7–8
  docs/god-view; all five pinned hashes LF-identical, timing values
  re-verified 5/4/8/13). Both committed alone. Live gate exit 0 at step
  0, at release banking, and immediately before banking this verdict.
- Vision-critical judgment: session model `us.anthropic.claude-fable-5`
  (verified from `PI_MODEL`, not self-report), on the committed sheet
  plus deterministic band crops of the same bytes (ephemeral
  diagnostics; native 1x decides). Council seat cross-vendor (Kimi
  K2.5), one consolidated adversarial verdict, 3,374 tokens of the 8k
  cap (1,621 in / 1,753 out, single call), response redirected to a
  file and read as UTF-8.

Question under test (the banked v6 next-hypothesis): does a one-tick
rise breakdown (r0→x0→idle, x0 consuming recovery tick 8) make the
ready-again completion — the grammar's last un-smoothed pose-only
discontinuity, and unlike v6's targets a banked SIGNAL (v5: the
attack/dodge unlock beat) — read as a rise rather than a swap, without
erasing the completion signal, without direction ambiguity against the
banked bridges, and without weakening any banked read? Decision rule
(pre-registered): B wins ONLY if rubric lines 1–3 pass AND lines 4–5
hold sheet-wide; otherwise REJECT and bank the incumbent snap as
correct salience — a first-class outcome.

## Accuracy — all-must-pass

| Item | Verdict | Evidence |
|---|---|---|
| 32x32 RGBA8, hard alpha, frozen 5-color ramp, declared palettes | PASS | asset gate + exporter pixel verification, both x0 poses |
| Occupied pixels inside `[2,2,29,29]`, anchor `[16,30]` | PASS | spec validation + gate bbox (down `[10,5,23,27]`, right `[7,6,29,27]` — col-29 edge shared with the banked r0-right precedent) |
| Head/eye cluster byte-exact at the declared VIRGIN translation | PASS | test-enforced: down = idle rows 4–14 at (+1,+1); right = idle rows 4–9 at (+1,+2); virginity against all ten banked translations test-enforced |
| Feet row 27, 0 drift; ramp unchanged; no gape authored | PASS | `rise_metrics --check` + spec-contract tests (accent-run bound 3px) |
| **Bridging bar** — max(d(r0,x0), d(x0,idle)) < d(r0,idle), per facing | PASS | down 21.45/20.43 < 37.09; right 19.67/23.62 < 37.64 — the largest gap yet bridged, max single-tick jump down 42%/37% vs the incumbent swap |
| **Nearest-neighbor bar** — two smallest grammar deltas = {r0, idle} | PASS | down: idle 20.43, r0 21.45 vs k0 26.42 (margin 4.97pt); right: r0 19.67, idle 23.62 vs k0 28.89 (margin 5.27pt) |
| **Identity ceiling** — every x0 delta < 44.44 | PASS | max 31.76 (down, vs f0) / 30.12 (right, vs f1); all walk + w0/s0 deltas reported |
| Mass monotone through the bridge (no pumping) | PASS | down 241→250→251; right 270→280→295 (down is idle-adjacent on mass — the pose divergence carries that bridge, disclosed in the rationale) |
| Frozen-state protection: six banked frame-sets byte-untouched | PASS | 22/22 export pins verified against banked release.json values (v0–v3, v5–v7) |
| **Release boundary IDENTICAL in A and B** (delta and +9px) | PASS | machine-compared: down 27.18/27.18, right 31.97/31.97; +9px step; poses pinned a0→k0 at t19→t20 in both |
| **Banked w0/s0 transition ticks IDENTICAL in A and B** | PASS | machine-compared: t15 = w0 and t24 = s0 in BOTH timelines (subsumed by tick-identity, checked explicitly — defense in depth) |
| Compositor byte-determinism (sheet, metrics, both APNGs) | PASS | double-build SHAs above; committed bytes equal fresh builds (sheet, metrics, both APNGs machine-compared) |
| Composition purity: every creature cell = banked export bytes at computed offsets | PASS | 298 cells, dual verification (region reconstruction + direct export-byte equality), 0 failures |
| Tick math exact: windup 5 at −3 (w0×1 + a0×4, BOTH timelines); active 4 at +6 (k0); recovery 8 at 0 (A: s0×1 + r0×7; B: s0×1 + r0×6 + x0×1); walk 13 at recomputed smoothstep; timelines tick-identical except t31 | PASS | `rise_metrics --check`, 0 failures; t31 machine-verified the ONLY differing tick |
| Pure pose boundary: t30–t33 all at position 32, offset 0 | PASS | tick plan (recovery else-branch and idle both 0); position deltas 0 in the jump table rows (30,31), (31,32), (32,33) |
| Release manifest complete, gate-valid at the live pin; specs validate; exports pixel-verified | PASS | gate over all seven releases, exit 0 |
| Banked v0–v6 sheets byte-stable; tools coverage ≥ 80 | PASS | full suite green (248 tests); coverage 94% (new tools 99% / 84%) |
| Pass bars pre-registered before artifacts existed | PASS | rationale committed (change set 1) before the sheet was generated (change set 2) |
| No game-two changes | PASS | read-only `git show` against `../game-two` only |

**Accuracy: 19/19.**

## Presentation — completion-rise rubric at native 1x

Measured evidence (`tools/rise_metrics.py`; silhouette delta =
100·XOR/union on export bytes; ticks are the contract, ms derived from
the unoverridden 16.666666 ms default):

| Boundary-jump table (pose Δ% / position Δpx) | down A | down B | right A | right B |
|---|---|---|---|---|
| t14→t15 (arrival→w0, banked v6) | 16.48 / −3 | 16.48 / −3 | 19.69 / −3 | 19.69 / −3 |
| t15→t16 (w0→a0, banked v6) | 17.25 / 0 | 17.25 / 0 | 20.83 / 0 | 20.83 / 0 |
| t19→t20 (**release, untouched**) | 27.18 / **+9** | 27.18 / **+9** | 31.97 / **+9** | 31.97 / **+9** |
| t23→t24 (k0→s0, banked v6) | 15.38 / −6 | 15.38 / −6 | 21.31 / −6 | 21.31 / −6 |
| t24→t25 (s0→r0, banked v6) | 16.18 / 0 | 16.18 / 0 | 14.98 / 0 | 14.98 / 0 |
| t30→t31 (held r0 → rise begins) | 0 / 0 | **21.45** / 0 | 0 / 0 | **19.67** / 0 |
| t31→t32 (**ready-again arrival**) | **37.09** / 0 | **20.43** / 0 | **37.64** / 0 | **23.62** / 0 |
| t32→t33 (held idle, context) | 0 / 0 | 0 / 0 | 0 / 0 | 0 / 0 |

Sharpest single-tick pose event: timeline A — the un-smoothed
ready-again swap (37.09/37.64); timeline B — the release (27.18/31.97,
also the only displaced boundary). The bridge steps are each 52–63% of
the direct swap (down 57.8%/55.1% — near-centered; right 52.3%/62.8% —
arrival-weighted, the deliberate completion-preserving shape declared
in the rationale). Timeline B's r0 hold after tick consumption: 6 ticks
= 100.0 ms (series 133.3 → 116.7 → 100.0 vs the KB ~150 ms reference —
the disclosed cost, stop-condition below). x0's bridge-neighbor deltas
(w0 20.88/19.87, s0 16.25/22.04) and walk deltas (16.25 is the
tightest cross-bridge proximity in the set, disclosed) are reported; no
floor applies to a bridge — the NN bar is the guard, and it passes with
margin everywhere.

Rubric line by line (pre-registered; judged on the committed sheet at
native 1x, both zones, both facings, with the 2X/RISE-4X strips and
band crops for diagnosis; every PASS is a static-sheet-scope claim —
per-tick legibility and single-tick recognizability under identical
pinned timing, never temporal fusion at 60 tps):

1. **The r0→x0→idle span in timeline B reads as one continuous rise
   where A's r0→idle is a single-tick full swap.** PASS at sheet scope,
   both facings, both zones, with council-corrected wording: the swap
   decomposes into two directionally consistent steps of 52–63% of the
   direct jump each (not "halves" — the right bridge is
   arrival-weighted by design). Down: the byte-exact head rises off the
   slump onto a base still carrying the right-side width residue, then
   the tall symmetric stance completes; right: the head lifts off the
   forward pool with the tail descending to mid, then the full re-form.
   Timeline A's t31→t32 column shows the full swap in one tick,
   side-by-side. The direction evidence is structural, not only metric:
   each head is a byte-exact rigid translation to a virgin intermediate
   offset (test-enforced), mass steps monotonically, and the RISE 4X
   strip reads top-down as lift → arrival in both facings.
2. **The completion signal survives.** PASS at sheet scope, the
   decision's load-bearing line, judged head-to-head as pre-registered.
   What v5 banked (quoted): "the boundary EXISTS in R and is
   single-tick recognizable… in A it does not exist — that is the
   point." Both properties survive in B: t31→t32 remains a single-tick,
   unmistakable state arrival at 1x (20.43/23.62 — at or above the
   whole banked v6 bridge-step band 14.98–21.31 that was judged
   per-tick readable, and the tall-symmetric-idle re-form is the
   highest-contrast pose class on the body). Head-to-head at native 1x:
   A's 37% snap is LOUDER, but it reads as a replacement — the
   slump-to-tall flip with no mechanical path; B's arrival reads as a
   completion — the rise begun at t31 lands. A does not read BETTER as
   a readiness signal at sheet scope; it reads bigger. **Disclosed
   trade, stated plainly (council kernel adopted):** B halves the
   beat's MAGNITUDE and cedes its sheet-extremum property to the
   release; whether the diminished beat still carries the unlock cue
   under motion is unprovable here and is elevated to the runtime item.
   The REJECT outcome was fully available on this line; the sheet
   evidence does not support it.
3. **x0 introduces no readable NEW state and no direction ambiguity.**
   PASS at sheet scope. In the eleven-column FILM rows x0 reads as
   between r0 and idle at 1x — head-high and narrowing against s0's
   head-low widening collapse (the pre-registered risk-1 opposition),
   rising at rest against w0's sinking at −3 (risk 2). The
   settle-family residues are visible at 4X exactly as declared: down —
   the +1 lateral head, the right-extended lower mass (w0's bulge
   extends LEFT), the right leg still at r0's column (w0's legs
   symmetric at idle's); right — the +1 forward head (w0 retracts −1),
   the tail one row above idle (w0's tail at idle rows), legs at r0's
   columns. The NN bar agrees with the native read everywhere. The
   s0-down proximity (16.25, the tightest cross-bridge delta in the
   set) is disclosed and pixel-adjudicated: at 1x and 4X the two read
   as opposite phases of the same family, and they never co-occur (six
   held r0 columns apart); the council's UNCERTAIN on this line is
   correct for a text-only reviewer — the pixel evidence carries it,
   and the 60 tps misread risk joins the runtime item.
4. **Untouched salience holds.** PASS at sheet scope, reframed per the
   council (adopted): the release is byte-identical in A and B
   (machine-compared, 27.18/31.97 at +9px), remains the only boundary
   combining pose change with displacement, and in B becomes the
   sheet-wide sharpest pose event — consistent with the v4-banked
   strike-dominance doctrine ("the sharpest motion in the sequence IS
   the strike"), which predates and was never displaced by v5's
   completion banking. The earlier framing "corrects the salience
   inversion" is DROPPED as council-flagged doublespeak: v5 never
   ranked the completion against the release, so there was no banked
   inversion to correct — what B does is trade the completion's
   extremum property for continuity, and that trade is exactly the
   sprint's declared tension, resolved by the pre-registered rule. The
   banked w0/s0 bridges read exactly as banked (byte-identical cells,
   machine-compared ticks).
5. **The banked reads survive the shortened hold.** PASS at sheet
   scope. The six identical r0 columns (t25–t30) still read as the held
   settle block at 1x in both zones and facings — the pre-registered
   REJECT trigger (settle read degrading at 6 columns) did not fire;
   s0's first-tick read is unchanged (byte-identical cell); identity
   holds throughout (byte-exact translated heads, frozen ramp, 0-drift
   feet, kept `oss` tail marker at mid-height on the right). The
   100.0 ms r0 hold vs the KB ~150 ms reference is the disclosed,
   pre-registered cost — recorded with a hard stop-condition below, not
   waved through.

**Presentation scores (accuracy scored separately above):**

- **Timeline B, down: 8.5/10.** Near-centered decomposition
  (21.45/20.43) of the largest boundary yet bridged; the RISE 4X strip
  reads as three frames of one motion with the residue story legible.
  Watch-item: the s0 cross-proximity (16.25) — direction-guarded in
  pose and context, temporally unproven.
- **Timeline B, right: 8/10.** The arrival-weighted shape (19.67/23.62)
  keeps the completion the strongest step in the bridge — the
  completion-preserving design choice working as intended; the facing
  echoes the banked v6 bridge-lean asymmetry (r0-adjacent bridges lean
  toward r0 on this facing).
- **Sheet as artifact: 8.5/10.** The single-boundary focus makes this
  the most legible A/B sheet of the series (one differing column,
  machine-verified); the four-pair DIFF row turns both pre-registered
  ambiguity risks into inspectable evidence.

## Structured critique and cross-vendor review (Kimi K2.5, adversarial)

One consolidated verdict; every numeric claim recomputed against
artifacts and pinned numbers before acceptance (models see text, not
pixels). Council posture was REJECT-overall; reconciled below.

1. **Q1 "roughly half" wording — REFUTED my wording, ADOPTED.** The
   right bridge is arrival-weighted (19.67/23.62 — ratio 0.83, steps
   52.3%/62.8% of direct), not "halves." Verified and corrected in
   rubric line 1; the asymmetry was pre-registered in the rationale as
   the deliberate completion-preserving shape, now the wording matches
   the numbers everywhere.
2. **Q2 L2 "redefines the signal" — REFUTED with the banked text,
   kernel ADOPTED.** The council's claim that v5 banked the
   "magnitude/extremum property" as the signal is fabricated-precedent
   class: v5's banked line (quoted verbatim in line 2 above) banks
   EXISTENCE + single-tick recognizability, both preserved in B. Its
   supporting arithmetic was also backwards — "23.62 falls below v6's
   accepted range 14.98–21.31" (23.62 is ABOVE the band's top, the
   survival-friendly direction). ADOPTED: the magnitude trade must be
   stated plainly rather than argued around — line 2 now carries it,
   and beat-strength-under-motion goes to the runtime item.
3. **Q3 s0 proximity — UNCERTAIN, correctly held.** The council's
   arithmetic (16.25 < 20.43: x0 sits closer to s0 than to idle) is
   correct and was disclosed. A text-only reviewer cannot adjudicate
   the residues; the pixel evidence (FILM/4X/DIFF) does, and passes.
   ADOPTED: the temporal s0-misread risk (a 16.7 ms x0 flashing as an
   s0-variant under motion) joins the runtime item.
4. **Q4 hold-erosion "laundering" — REFUTED as characterization,
   stop-condition ADOPTED (tick-corrected).** Each reduction was
   pre-registered in its sprint's rationale BEFORE artifacts with an
   explicit sheet-scope REJECT trigger — the opposite of laundering;
   and the engine-pinned recovery window (8 ticks) never changed, only
   its pose allocation. But the council's structural point is right:
   per-sprint disclosures do not compose into a limit. ADOPTED as a
   pre-registered stop-condition: **the 6-tick r0 hold is the FLOOR —
   no future sprint may consume another tick of this span for any
   in-between (the pinned window would drop to 83.3 ms held
   conclusion), and any proposal below 6 requires runtime replay
   evidence FIRST.** The council's proposed 125 ms floor was
   tick-incoherent (7.5 ticks) and is replaced by the tick-aligned
   floor.
5. **Q5 salience "regression" — PARTIALLY ADOPTED.** REFUTED as
   precedent: release-dominance is v4-banked doctrine ("the sharpest
   motion in the sequence IS the strike"), so B restoring the release
   as sheet-extremum contradicts no banked priority — v5 never ranked
   completion against release. ADOPTED: my "corrects the salience
   inversion" framing was doublespeak and is dropped (line 4); the
   completion ceding the extremum property is a deliberate trade with
   an untested runtime consequence, recorded as such.
6. **Q6 unthought risk — ADOPTED with arithmetic corrected.** Temporal
   aliasing: under motion the r0→x0 step (21.45/19.67) may fuse with
   the held r0 span, collapsing B's two-step rise into a single
   ~20/24% arrival — a WEAKER beat than the sheet suggests (the
   council's "apparent 37% jump" was backwards: fusion yields the
   x0→idle magnitude, not the direct swap's). Unprovable on a sheet;
   now the sharpest-stated entry in the runtime item. Secondary risk
   (future translation collisions): already the standing convention —
   every new state/bridge must re-verify translation virginity against
   the grown table (test-enforced per sprint); noted, no action.

No council claim survived re-verification that contradicts the
decision below at its pre-registered scope; two wording corrections and
one stop-condition were adopted from it, and its REJECT recommendation
rested materially on the Q2 fabricated banking claim and the Q4/Q5
characterizations refuted against the banked texts.

## Decision

**B WINS at the pre-registered artifact scope — BANK x0. The
single-boundary sub-verdict: the completion-rise hypothesis is answered
YES at asset level, with the trade recorded.**

- **Ready-again (x0): BANK.** The grammar's largest single-tick
  discontinuity (37.09/37.64, pose-only, 0px) decomposes into two
  directionally consistent steps (21.45/20.43 down — near-centered;
  19.67/23.62 right — arrival-weighted by design), with the
  nearest-neighbor guard passing both facings (margins 4.97/5.27pt),
  both pre-registered ambiguity risks (s0-direction, w0-direction)
  defeated at static scope by pose residues + the NN bar, the release
  and banked v6 bridges machine-proven untouched, and the held-settle
  read surviving six columns. Rubric lines 1–3 pass; lines 4–5 hold
  sheet-wide.
- **What B trades, plainly:** the completion beat keeps its v5-banked
  properties (exists; single-tick recognizable) but loses its
  sheet-extremum magnitude to the release (the v4-banked strike
  salience). The incumbent snap remains fully recoverable — the frozen
  frames are untouched and timeline A regenerates byte-identically —
  so a runtime-replay failure of the diminished beat reverses this
  banking without pixel loss.

What this sprint does **not** claim: temporal fusion at 60 tps (whether
the one-tick rise fuses as motion or strobes), beat-STRENGTH under
motion (whether a ~20/24% arrival still functions as the attack/dodge
unlock cue at speed — the council's strongest kernel, now first in the
runtime item), duration sufficiency of the 100.0 ms r0 hold, or any
runtime integration — those are runtime replay-capture questions,
blocked by the one-way boundary.

Status per contract: **selectable candidates only.** No integration —
v18 is open in game-two; the one-way boundary and every
runtime-integration stop condition in `docs/asset-contract.md` hold.
The lane-B attack grammar now carries five banked states plus three
banked transition keys per facing (16 creature frames total), and every
grammar boundary except the deliberately-sharp release is bridged.

## Findings for any future integration design (recorded, no action now)

- **Moving-recovery pose priority** (carried, v5/v6): walking is legal
  during recovery; walk-vs-r0 priority undecided — x0 adds a third
  sub-case (walk input during recovery tick 8: does the rise or the
  walk frame win?).
- **Exhaust-tail invisibility** (carried, v5): ~18 post-recovery locked
  ticks remain unmarkable asset-side; x0 does not reach them (recovery
  tick 8 is the last drawn attack_state tick).
- **Runtime temporal confirmation (single consolidated item, extended
  — now the gating question for this sprint's banking):** replay
  capture should verify (a) **the diminished ready-again beat still
  reads as the unlock cue at 60 tps (20.43/23.62 arrival vs the
  incumbent 37.09/37.64 snap) and the r0→x0 step does not fuse into the
  held r0 span, collapsing the two-step rise into a single weaker
  beat** (council Q6, adopted); (b) x0 at 16.7 ms is not misread as an
  s0-variant mid-motion (the 16.25 down proximity); (c) the r0 hold
  reads as settle at 100.0 ms (was 133.3 → 116.7 — the cumulative
  trade); (d) the carried v6 checks — w0/s0 strobe/fusion, w0
  coil-onset binding, 66.7 ms a0 hold; (e) the k0→s0 −6px return reads
  as recoil; (f) the ACC flash-accent under live flicker (carried v4).
  If (a) fails, REVERSE the x0 banking (timeline A is byte-recoverable)
  rather than compensating elsewhere.
- **r0-hold floor (new, pre-registered stop-condition, council Q4
  adopted tick-aligned):** 6 held r0 ticks (100.0 ms) is the FLOOR for
  this span. No future in-between may consume another recovery tick;
  any proposal below the floor requires the runtime replay evidence
  first. The engine-side remedy attribution (v4 EXP-row: duration fixes
  live in the timing constants) is unchanged.
- **Facing-mirror handedness** (carried, v5/v6, extended): x0-down's
  lateral head residue and off-home right leg encode the same
  handedness as the r0/s0 slump family; a mirrored facing set must
  re-verify all three together.
- **Enemy-side reuse** (carried, v4–v6): divided-attention
  re-verification extends to x0 — on an enemy body the diminished
  ready-again beat is a punish-window cue, where the magnitude trade
  may bind differently than on the player's own body.
- **Bridge-lean asymmetry** (carried, v6, extended): the right-facing
  bridges consistently lean toward r0 (s0: 14.98 settle-ward; x0:
  19.67 rise-start) while down stays near-centered — a facing-family
  property now seen twice; any settle-family regularization must
  re-verify both leans stay endpoint-nearest.
- **Translation-registry discipline** (council Q6 secondary, standing):
  the byte-exact head-translation table now carries six entries per
  facing; every future state/bridge must test-enforce virginity against
  the grown table (the x0 tests added the explicit virginity guard —
  keep it in every future spec-contract suite).

## Next hypothesis (single, for a future bounded sprint — not started)

The lane-B attack grammar is now closed at asset level: five states,
three bridges, one deliberately-sharp release — no un-smoothed
single-tick pose discontinuity remains that any banked verdict marks as
noise. The remaining asset-side unknowns are cross-grammar, not
intra-attack: the walk↔attack seams under real input (arrival
walk-frame → w0 at any walk phase, not just the f3 arrival the timeline
models; recovery-walk pose priority) — BUT the highest-value next step
is not a new frame at all: the consolidated runtime replay item now
carries three sprints of temporal questions with the x0
banking-reversal condition at its head, and the one-way boundary is the
only thing between the banked A|B APNGs and a live-capture answer.
Recommendation: the next bounded effort should be the
runtime-replay-capture integration design (contract-permitted: a
game-two-side change AFTER v18 closes, loading the banked frames in a
replay harness without touching simulation identity), not an eighth
calibration release. If a calibration sprint happens first, the
walk-phase→w0 seam is the only pose-only discontinuity class left
unmeasured.

## Stop

Sprint 7 stops here: one reviewed timeline sheet, one banked verdict,
two non-blocking APNG aids, two new creature frames (the x0 pair), one
gate-valid release, two re-pin commits (one dev-seat-protocol content
re-baseline with the full rider verification table, one mechanical
identity re-pin) plus two bounded change sets. No smear frames, no
added ticks, no release smoothing, no second in-between on this
boundary, no exhaust-state pose, no 8-direction set, no terrain, no
enemies, no pack, no game-two changes.
